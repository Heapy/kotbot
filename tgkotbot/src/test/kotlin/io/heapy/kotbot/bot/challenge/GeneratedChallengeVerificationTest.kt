package io.heapy.kotbot.bot.challenge

import org.junit.jupiter.api.Assertions.assertEquals
import org.junit.jupiter.api.Test

/**
 * Auto-generated test that executes challenge snippets and verifies correctness.
 * Generated by [ChallengeSnippetVerificationGenerator].
 * DO NOT EDIT MANUALLY.
 */
class GeneratedChallengeVerificationTest {

    private fun verify(expected: String, actual: Any?, seed: Long, snippet: String) {
        assertEquals(expected, "$actual", "seed=$seed snippet=$snippet")
    }

    @Test
    fun `verify collection_pipeline batch 0`() {
        verify("34", run { listOf(14, 11, 17, 14).filter { it > 14 }.map { it * 2 }.sum() }, 0L, "listOf(14, 11, 17, 14).filter { it > 14 }.map { it * 2 }.sum()")
        verify("100", run { listOf(14, 17, 19, 4).filter { it > 4 }.map { it * 2 }.sum() }, 0L, "listOf(14, 17, 19, 4).filter { it > 4 }.map { it * 2 }.sum()")
        verify("3", run { listOf(7, 22, 8, 24, 12).filter { it % 4 == 0 }.size }, 0L, "listOf(7, 22, 8, 24, 12).filter { it % 4 == 0 }.size")
        verify("60", run { listOf(11, 13, 17, 11).filter { it > 11 }.map { it * 2 }.sum() }, 0L, "listOf(11, 13, 17, 11).filter { it > 11 }.map { it * 2 }.sum()")
        verify("50", run { listOf(2, 6, 19, 1).filter { it > 2 }.map { it * 2 }.sum() }, 0L, "listOf(2, 6, 19, 1).filter { it > 2 }.map { it * 2 }.sum()")
        verify("[0, 1, 2]", run { listOf(8, 4, 4, 1, 8).map { it / 3 }.distinct().sorted() }, 0L, "listOf(8, 4, 4, 1, 8).map { it / 3 }.distinct().sorted()")
        verify("28", run { listOf(13, 4, 9, 14, 4, 9).filter { it > 13 }.map { it * 2 }.sum() }, 0L, "listOf(13, 4, 9, 14, 4, 9).filter { it > 13 }.map { it * 2 }.sum()")
        verify("[0, 1, 2, 3]", run { listOf(7, 6, 9, 5, 4, 9, 2).map { it / 3 }.distinct().sorted() }, 0L, "listOf(7, 6, 9, 5, 4, 9, 2).map { it / 3 }.distinct().sorted()")
        verify("2", run { listOf(17, 21, 11, 21, 16).filter { it % 3 == 0 }.size }, 0L, "listOf(17, 21, 11, 21, 16).filter { it % 3 == 0 }.size")
        verify("2", run { listOf(2, 18, 13, 12, 19, 10, 10).filter { it % 3 == 0 }.size }, 0L, "listOf(2, 18, 13, 12, 19, 10, 10).filter { it % 3 == 0 }.size")
        verify("104", run { listOf(13, 10, 17, 11, 11).filter { it > 10 }.map { it * 2 }.sum() }, 0L, "listOf(13, 10, 17, 11, 11).filter { it > 10 }.map { it * 2 }.sum()")
        verify("[0, 1, 2]", run { listOf(4, 6, 7, 4, 2, 1).map { it / 3 }.distinct().sorted() }, 0L, "listOf(4, 6, 7, 4, 2, 1).map { it / 3 }.distinct().sorted()")
        verify("2", run { listOf(5, 28, 11, 10, 27).filter { it % 2 == 0 }.size }, 0L, "listOf(5, 28, 11, 10, 27).filter { it % 2 == 0 }.size")
        verify("1", run { listOf(25, 5, 16, 6, 25).filter { it % 4 == 0 }.size }, 0L, "listOf(25, 5, 16, 6, 25).filter { it % 4 == 0 }.size")
        verify("2", run { listOf(28, 21, 5, 20, 9).filter { it % 4 == 0 }.size }, 0L, "listOf(28, 21, 5, 20, 9).filter { it % 4 == 0 }.size")
        verify("2", run { listOf(3, 23, 4, 27, 19, 24, 17).filter { it % 4 == 0 }.size }, 0L, "listOf(3, 23, 4, 27, 19, 24, 17).filter { it % 4 == 0 }.size")
        verify("72", run { listOf(10, 7, 8, 4, 7, 18).filter { it > 7 }.map { it * 2 }.sum() }, 0L, "listOf(10, 7, 8, 4, 7, 18).filter { it > 7 }.map { it * 2 }.sum()")
        verify("[1, 2, 3, 4]", run { listOf(2, 6, 8, 9, 4).map { it / 2 }.distinct().sorted() }, 0L, "listOf(2, 6, 8, 9, 4).map { it / 2 }.distinct().sorted()")
        verify("3", run { listOf(13, 27, 21, 18, 8, 5).filter { it % 3 == 0 }.size }, 0L, "listOf(13, 27, 21, 18, 8, 5).filter { it % 3 == 0 }.size")
        verify("2", run { listOf(1, 24, 29, 24, 5, 14).filter { it % 4 == 0 }.size }, 0L, "listOf(1, 24, 29, 24, 5, 14).filter { it % 4 == 0 }.size")
        verify("84", run { listOf(4, 10, 19, 13).filter { it > 4 }.map { it * 2 }.sum() }, 0L, "listOf(4, 10, 19, 13).filter { it > 4 }.map { it * 2 }.sum()")
        verify("38", run { listOf(19, 4, 1, 5).filter { it > 5 }.map { it * 2 }.sum() }, 0L, "listOf(19, 4, 1, 5).filter { it > 5 }.map { it * 2 }.sum()")
        verify("[0, 2, 3]", run { listOf(6, 6, 6, 9, 7, 2, 8).map { it / 3 }.distinct().sorted() }, 0L, "listOf(6, 6, 6, 9, 7, 2, 8).map { it / 3 }.distinct().sorted()")
        verify("50", run { listOf(6, 14, 11, 8).filter { it > 8 }.map { it * 2 }.sum() }, 0L, "listOf(6, 14, 11, 8).filter { it > 8 }.map { it * 2 }.sum()")
        verify("74", run { listOf(10, 1, 8, 7, 19, 4).filter { it > 7 }.map { it * 2 }.sum() }, 0L, "listOf(10, 1, 8, 7, 19, 4).filter { it > 7 }.map { it * 2 }.sum()")
        verify("110", run { listOf(5, 2, 18, 11, 11, 10).filter { it > 2 }.map { it * 2 }.sum() }, 0L, "listOf(5, 2, 18, 11, 11, 10).filter { it > 2 }.map { it * 2 }.sum()")
        verify("[2, 3, 4]", run { listOf(9, 8, 7, 4, 4).map { it / 2 }.distinct().sorted() }, 0L, "listOf(9, 8, 7, 4, 4).map { it / 2 }.distinct().sorted()")
        verify("0", run { listOf(11, 14, 16, 9, 9).filter { it > 16 }.map { it * 2 }.sum() }, 0L, "listOf(11, 14, 16, 9, 9).filter { it > 16 }.map { it * 2 }.sum()")
        verify("100", run { listOf(7, 1, 10, 19, 14, 17).filter { it > 10 }.map { it * 2 }.sum() }, 0L, "listOf(7, 1, 10, 19, 14, 17).filter { it > 10 }.map { it * 2 }.sum()")
        verify("[0, 1, 2, 3]", run { listOf(7, 2, 9, 4, 7).map { it / 3 }.distinct().sorted() }, 0L, "listOf(7, 2, 9, 4, 7).map { it / 3 }.distinct().sorted()")
        verify("1", run { listOf(7, 8, 14, 6, 10).filter { it % 3 == 0 }.size }, 0L, "listOf(7, 8, 14, 6, 10).filter { it % 3 == 0 }.size")
        verify("2", run { listOf(5, 23, 3, 26, 20, 15, 16).filter { it % 4 == 0 }.size }, 0L, "listOf(5, 23, 3, 26, 20, 15, 16).filter { it % 4 == 0 }.size")
        verify("[1, 2, 4]", run { listOf(4, 4, 3, 9, 3).map { it / 2 }.distinct().sorted() }, 0L, "listOf(4, 4, 3, 9, 3).map { it / 2 }.distinct().sorted()")
        verify("90", run { listOf(4, 6, 19, 4, 8, 12).filter { it > 4 }.map { it * 2 }.sum() }, 0L, "listOf(4, 6, 19, 4, 8, 12).filter { it > 4 }.map { it * 2 }.sum()")
        verify("[1, 2, 3, 4]", run { listOf(6, 3, 5, 3, 8, 9).map { it / 2 }.distinct().sorted() }, 0L, "listOf(6, 3, 5, 3, 8, 9).map { it / 2 }.distinct().sorted()")
        verify("82", run { listOf(12, 12, 6, 2, 11).filter { it > 2 }.map { it * 2 }.sum() }, 0L, "listOf(12, 12, 6, 2, 11).filter { it > 2 }.map { it * 2 }.sum()")
        verify("[2, 3, 4]", run { listOf(9, 4, 6, 7, 6).map { it / 2 }.distinct().sorted() }, 0L, "listOf(9, 4, 6, 7, 6).map { it / 2 }.distinct().sorted()")
        verify("28", run { listOf(6, 3, 7, 14, 7).filter { it > 7 }.map { it * 2 }.sum() }, 0L, "listOf(6, 3, 7, 14, 7).filter { it > 7 }.map { it * 2 }.sum()")
        verify("4", run { listOf(28, 20, 9, 28, 7, 9, 20).filter { it % 2 == 0 }.size }, 0L, "listOf(28, 20, 9, 28, 7, 9, 20).filter { it % 2 == 0 }.size")
        verify("3", run { listOf(29, 8, 25, 14, 3, 12, 29).filter { it % 2 == 0 }.size }, 0L, "listOf(29, 8, 25, 14, 3, 12, 29).filter { it % 2 == 0 }.size")
        verify("40", run { listOf(6, 2, 12, 1).filter { it > 1 }.map { it * 2 }.sum() }, 0L, "listOf(6, 2, 12, 1).filter { it > 1 }.map { it * 2 }.sum()")
        verify("3", run { listOf(17, 10, 13, 22, 11, 11, 6).filter { it % 2 == 0 }.size }, 0L, "listOf(17, 10, 13, 22, 11, 11, 6).filter { it % 2 == 0 }.size")
        verify("[0, 2]", run { listOf(6, 6, 2, 7, 8).map { it / 3 }.distinct().sorted() }, 0L, "listOf(6, 6, 2, 7, 8).map { it / 3 }.distinct().sorted()")
        verify("18", run { listOf(5, 9, 4, 1, 5).filter { it > 5 }.map { it * 2 }.sum() }, 0L, "listOf(5, 9, 4, 1, 5).filter { it > 5 }.map { it * 2 }.sum()")
        verify("[0, 1, 2]", run { listOf(1, 4, 6, 5, 8).map { it / 3 }.distinct().sorted() }, 0L, "listOf(1, 4, 6, 5, 8).map { it / 3 }.distinct().sorted()")
        verify("5", run { listOf(4, 7, 2, 14, 8, 23, 26).filter { it % 2 == 0 }.size }, 0L, "listOf(4, 7, 2, 14, 8, 23, 26).filter { it % 2 == 0 }.size")
        verify("[0, 1, 2]", run { listOf(3, 1, 5, 3, 2, 7, 4).map { it / 3 }.distinct().sorted() }, 0L, "listOf(3, 1, 5, 3, 2, 7, 4).map { it / 3 }.distinct().sorted()")
        verify("70", run { listOf(8, 19, 3, 16).filter { it > 8 }.map { it * 2 }.sum() }, 0L, "listOf(8, 19, 3, 16).filter { it > 8 }.map { it * 2 }.sum()")
        verify("144", run { listOf(17, 5, 17, 7, 19, 19).filter { it > 7 }.map { it * 2 }.sum() }, 0L, "listOf(17, 5, 17, 7, 19, 19).filter { it > 7 }.map { it * 2 }.sum()")
        verify("2", run { listOf(24, 2, 29, 17, 15).filter { it % 2 == 0 }.size }, 0L, "listOf(24, 2, 29, 17, 15).filter { it % 2 == 0 }.size")
    }

    @Test
    fun `verify collection_pipeline batch 1`() {
        verify("4", run { listOf(26, 21, 14, 3, 2, 20).filter { it % 2 == 0 }.size }, 0L, "listOf(26, 21, 14, 3, 2, 20).filter { it % 2 == 0 }.size")
        verify("0", run { listOf(13, 18, 3, 11).filter { it > 18 }.map { it * 2 }.sum() }, 0L, "listOf(13, 18, 3, 11).filter { it > 18 }.map { it * 2 }.sum()")
        verify("1", run { listOf(1, 19, 8, 1, 13, 12).filter { it % 3 == 0 }.size }, 0L, "listOf(1, 19, 8, 1, 13, 12).filter { it % 3 == 0 }.size")
        verify("32", run { listOf(16, 4, 5, 5, 10, 10).filter { it > 10 }.map { it * 2 }.sum() }, 0L, "listOf(16, 4, 5, 5, 10, 10).filter { it > 10 }.map { it * 2 }.sum()")
        verify("34", run { listOf(17, 10, 10, 13, 3, 2).filter { it > 13 }.map { it * 2 }.sum() }, 0L, "listOf(17, 10, 10, 13, 3, 2).filter { it > 13 }.map { it * 2 }.sum()")
        verify("86", run { listOf(6, 7, 18, 1, 18).filter { it > 6 }.map { it * 2 }.sum() }, 0L, "listOf(6, 7, 18, 1, 18).filter { it > 6 }.map { it * 2 }.sum()")
        verify("3", run { listOf(21, 4, 18, 28, 24).filter { it % 4 == 0 }.size }, 0L, "listOf(21, 4, 18, 28, 24).filter { it % 4 == 0 }.size")
        verify("38", run { listOf(13, 3, 19, 2).filter { it > 13 }.map { it * 2 }.sum() }, 0L, "listOf(13, 3, 19, 2).filter { it > 13 }.map { it * 2 }.sum()")
        verify("4", run { listOf(17, 12, 18, 17, 9, 6, 20).filter { it % 2 == 0 }.size }, 0L, "listOf(17, 12, 18, 17, 9, 6, 20).filter { it % 2 == 0 }.size")
        verify("3", run { listOf(18, 19, 4, 11, 24, 27).filter { it % 3 == 0 }.size }, 0L, "listOf(18, 19, 4, 11, 24, 27).filter { it % 3 == 0 }.size")
        verify("[0, 1, 3]", run { listOf(6, 3, 1, 2, 6).map { it / 2 }.distinct().sorted() }, 0L, "listOf(6, 3, 1, 2, 6).map { it / 2 }.distinct().sorted()")
        verify("36", run { listOf(6, 9, 4, 18, 15, 15).filter { it > 15 }.map { it * 2 }.sum() }, 0L, "listOf(6, 9, 4, 18, 15, 15).filter { it > 15 }.map { it * 2 }.sum()")
        verify("1", run { listOf(19, 22, 23, 10, 15).filter { it % 3 == 0 }.size }, 0L, "listOf(19, 22, 23, 10, 15).filter { it % 3 == 0 }.size")
        verify("74", run { listOf(3, 8, 13, 3, 16, 1).filter { it > 3 }.map { it * 2 }.sum() }, 0L, "listOf(3, 8, 13, 3, 16, 1).filter { it > 3 }.map { it * 2 }.sum()")
        verify("2", run { listOf(9, 19, 25, 9, 24, 28).filter { it % 2 == 0 }.size }, 0L, "listOf(9, 19, 25, 9, 24, 28).filter { it % 2 == 0 }.size")
        verify("1", run { listOf(27, 7, 6, 13, 7, 8).filter { it % 4 == 0 }.size }, 0L, "listOf(27, 7, 6, 13, 7, 8).filter { it % 4 == 0 }.size")
        verify("[1, 2]", run { listOf(5, 3, 6, 4, 3).map { it / 3 }.distinct().sorted() }, 0L, "listOf(5, 3, 6, 4, 3).map { it / 3 }.distinct().sorted()")
        verify("0", run { listOf(13, 19, 8, 14).filter { it > 19 }.map { it * 2 }.sum() }, 0L, "listOf(13, 19, 8, 14).filter { it > 19 }.map { it * 2 }.sum()")
        verify("[1, 3, 4]", run { listOf(2, 8, 9, 8, 8, 3, 6).map { it / 2 }.distinct().sorted() }, 0L, "listOf(2, 8, 9, 8, 8, 3, 6).map { it / 2 }.distinct().sorted()")
        verify("[0, 1, 3, 4]", run { listOf(9, 8, 3, 1, 6, 9).map { it / 2 }.distinct().sorted() }, 0L, "listOf(9, 8, 3, 1, 6, 9).map { it / 2 }.distinct().sorted()")
        verify("68", run { listOf(15, 4, 19, 11).filter { it > 11 }.map { it * 2 }.sum() }, 0L, "listOf(15, 4, 19, 11).filter { it > 11 }.map { it * 2 }.sum()")
        verify("42", run { listOf(1, 1, 7, 5, 4, 5).filter { it > 1 }.map { it * 2 }.sum() }, 0L, "listOf(1, 1, 7, 5, 4, 5).filter { it > 1 }.map { it * 2 }.sum()")
        verify("[1, 2, 3]", run { listOf(4, 6, 8, 4, 6, 9, 8).map { it / 3 }.distinct().sorted() }, 0L, "listOf(4, 6, 8, 4, 6, 9, 8).map { it / 3 }.distinct().sorted()")
        verify("[0, 1, 3, 4]", run { listOf(6, 3, 6, 6, 9, 1, 1).map { it / 2 }.distinct().sorted() }, 0L, "listOf(6, 3, 6, 6, 9, 1, 1).map { it / 2 }.distinct().sorted()")
        verify("[2, 3, 4]", run { listOf(8, 8, 5, 5, 5, 7).map { it / 2 }.distinct().sorted() }, 0L, "listOf(8, 8, 5, 5, 5, 7).map { it / 2 }.distinct().sorted()")
        verify("86", run { listOf(6, 8, 17, 18).filter { it > 6 }.map { it * 2 }.sum() }, 0L, "listOf(6, 8, 17, 18).filter { it > 6 }.map { it * 2 }.sum()")
        verify("62", run { listOf(9, 8, 9, 6, 13, 4).filter { it > 8 }.map { it * 2 }.sum() }, 0L, "listOf(9, 8, 9, 6, 13, 4).filter { it > 8 }.map { it * 2 }.sum()")
        verify("68", run { listOf(12, 6, 16, 18).filter { it > 12 }.map { it * 2 }.sum() }, 0L, "listOf(12, 6, 16, 18).filter { it > 12 }.map { it * 2 }.sum()")
        verify("0", run { listOf(10, 17, 14, 7, 10).filter { it % 4 == 0 }.size }, 0L, "listOf(10, 17, 14, 7, 10).filter { it % 4 == 0 }.size")
        verify("0", run { listOf(23, 1, 27, 29, 13, 23).filter { it % 4 == 0 }.size }, 0L, "listOf(23, 1, 27, 29, 13, 23).filter { it % 4 == 0 }.size")
        verify("2", run { listOf(10, 16, 14, 24, 11, 27, 26).filter { it % 4 == 0 }.size }, 0L, "listOf(10, 16, 14, 24, 11, 27, 26).filter { it % 4 == 0 }.size")
        verify("1", run { listOf(19, 25, 8, 9, 26).filter { it % 4 == 0 }.size }, 0L, "listOf(19, 25, 8, 9, 26).filter { it % 4 == 0 }.size")
        verify("[0, 2, 3, 4]", run { listOf(5, 8, 1, 9, 6, 9, 8).map { it / 2 }.distinct().sorted() }, 0L, "listOf(5, 8, 1, 9, 6, 9, 8).map { it / 2 }.distinct().sorted()")
        verify("0", run { listOf(29, 1, 11, 14, 23).filter { it % 3 == 0 }.size }, 0L, "listOf(29, 1, 11, 14, 23).filter { it % 3 == 0 }.size")
        verify("30", run { listOf(4, 15, 14, 7).filter { it > 14 }.map { it * 2 }.sum() }, 0L, "listOf(4, 15, 14, 7).filter { it > 14 }.map { it * 2 }.sum()")
        verify("[0, 1, 2, 3]", run { listOf(8, 1, 4, 4, 9).map { it / 3 }.distinct().sorted() }, 0L, "listOf(8, 1, 4, 4, 9).map { it / 3 }.distinct().sorted()")
        verify("[0, 2, 3, 4]", run { listOf(4, 9, 9, 6, 1).map { it / 2 }.distinct().sorted() }, 0L, "listOf(4, 9, 9, 6, 1).map { it / 2 }.distinct().sorted()")
        verify("66", run { listOf(19, 12, 5, 14, 11, 8).filter { it > 12 }.map { it * 2 }.sum() }, 0L, "listOf(19, 12, 5, 14, 11, 8).filter { it > 12 }.map { it * 2 }.sum()")
        verify("60", run { listOf(16, 14, 2, 5).filter { it > 5 }.map { it * 2 }.sum() }, 0L, "listOf(16, 14, 2, 5).filter { it > 5 }.map { it * 2 }.sum()")
        verify("[1, 2, 3]", run { listOf(3, 6, 5, 8, 9, 6).map { it / 3 }.distinct().sorted() }, 0L, "listOf(3, 6, 5, 8, 9, 6).map { it / 3 }.distinct().sorted()")
        verify("0", run { listOf(7, 11, 2, 17).filter { it > 17 }.map { it * 2 }.sum() }, 0L, "listOf(7, 11, 2, 17).filter { it > 17 }.map { it * 2 }.sum()")
        verify("2", run { listOf(25, 14, 25, 27, 21, 11, 16).filter { it % 3 == 0 }.size }, 0L, "listOf(25, 14, 25, 27, 21, 11, 16).filter { it % 3 == 0 }.size")
        verify("[0, 1]", run { listOf(5, 1, 1, 5, 1).map { it / 3 }.distinct().sorted() }, 0L, "listOf(5, 1, 1, 5, 1).map { it / 3 }.distinct().sorted()")
        verify("0", run { listOf(10, 14, 14, 1, 9).filter { it > 14 }.map { it * 2 }.sum() }, 0L, "listOf(10, 14, 14, 1, 9).filter { it > 14 }.map { it * 2 }.sum()")
        verify("[0, 1, 2]", run { listOf(4, 2, 5, 8, 6).map { it / 3 }.distinct().sorted() }, 0L, "listOf(4, 2, 5, 8, 6).map { it / 3 }.distinct().sorted()")
        verify("[1, 2, 3]", run { listOf(2, 2, 6, 4, 3).map { it / 2 }.distinct().sorted() }, 0L, "listOf(2, 2, 6, 4, 3).map { it / 2 }.distinct().sorted()")
        verify("2", run { listOf(26, 9, 4, 5, 5, 22, 6).filter { it % 3 == 0 }.size }, 0L, "listOf(26, 9, 4, 5, 5, 22, 6).filter { it % 3 == 0 }.size")
        verify("[1, 2]", run { listOf(4, 2, 3, 2, 4, 2).map { it / 2 }.distinct().sorted() }, 0L, "listOf(4, 2, 3, 2, 4, 2).map { it / 2 }.distinct().sorted()")
        verify("88", run { listOf(15, 12, 8, 9, 1).filter { it > 1 }.map { it * 2 }.sum() }, 0L, "listOf(15, 12, 8, 9, 1).filter { it > 1 }.map { it * 2 }.sum()")
        verify("2", run { listOf(19, 15, 13, 28, 25, 26).filter { it % 2 == 0 }.size }, 0L, "listOf(19, 15, 13, 28, 25, 26).filter { it % 2 == 0 }.size")
    }

    @Test
    fun `verify string_operations batch 0`() {
        verify("OU", run { "coroutine".substring(3, 5).uppercase() }, 0L, "\"coroutine\".substring(3, 5).uppercase()")
        verify("UE", run { "sequence".substring(3, 5).uppercase() }, 0L, "\"sequence\".substring(3, 5).uppercase()")
        verify("pdoc", run { "code".replace('e', 'p').reversed() }, 0L, "\"code\".replace('e', 'p').reversed()")
        verify("UE", run { "sequence".substring(3, 5).uppercase() }, 0L, "\"sequence\".substring(3, 5).uppercase()")
        verify("KO", run { "kotlin".substring(0, 2).uppercase() }, 0L, "\"kotlin\".substring(0, 2).uppercase()")
        verify("nc", run { "functional".drop(2).take(2) }, 0L, "\"functional\".drop(2).take(2)")
        verify("OTLI", run { "kotlin".substring(1, 5).uppercase() }, 0L, "\"kotlin\".substring(1, 5).uppercase()")
        verify("eleg", run { "delegate".drop(1).take(4) }, 0L, "\"delegate\".drop(1).take(4)")
        verify("edox", run { "code".replace('c', 'x').reversed() }, 0L, "\"code\".replace('c', 'x').reversed()")
        verify("ealav", run { "value".replace('u', 'a').reversed() }, 0L, "\"value\".replace('u', 'a').reversed()")
        verify("SUS", run { "suspend".substring(0, 3).uppercase() }, 0L, "\"suspend\".substring(0, 3).uppercase()")
        verify("unct", run { "functional".drop(1).take(4) }, 0L, "\"functional\".drop(1).take(4)")
        verify("edhc", run { "code".replace('o', 'h').reversed() }, 0L, "\"code\".replace('o', 'h').reversed()")
        verify("edac", run { "code".replace('o', 'a').reversed() }, 0L, "\"code\".replace('o', 'a').reversed()")
        verify("otteh", run { "hello".replace('l', 't').reversed() }, 0L, "\"hello\".replace('l', 't').reversed()")
        verify("uiltok", run { "kotlin".replace('n', 'u').reversed() }, 0L, "\"kotlin\".replace('n', 'u').reversed()")
        verify("OUTI", run { "coroutine".substring(3, 7).uppercase() }, 0L, "\"coroutine\".substring(3, 7).uppercase()")
        verify("tens", run { "extension".drop(2).take(4) }, 0L, "\"extension\".drop(2).take(4)")
        verify("ltld", run { "data".replace('a', 'l').reversed() }, 0L, "\"data\".replace('a', 'l').reversed()")
        verify("stsd", run { "data".replace('a', 's').reversed() }, 0L, "\"data\".replace('a', 's').reversed()")
        verify("OTL", run { "kotlin".substring(1, 4).uppercase() }, 0L, "\"kotlin\".substring(1, 4).uppercase()")
        verify("COMP", run { "compose".substring(0, 4).uppercase() }, 0L, "\"compose\".substring(0, 4).uppercase()")
        verify("ensi", run { "extension".drop(3).take(4) }, 0L, "\"extension\".drop(3).take(4)")
        verify("OMP", run { "compose".substring(1, 4).uppercase() }, 0L, "\"compose\".substring(1, 4).uppercase()")
        verify("US", run { "suspend".substring(1, 3).uppercase() }, 0L, "\"suspend\".substring(1, 3).uppercase()")
        verify("LAM", run { "lambda".substring(0, 3).uppercase() }, 0L, "\"lambda\".substring(0, 3).uppercase()")
        verify("uta", run { "immutable".drop(3).take(3) }, 0L, "\"immutable\".drop(3).take(3)")
        verify("LAMB", run { "lambda".substring(0, 4).uppercase() }, 0L, "\"lambda\".substring(0, 4).uppercase()")
        verify("ANDR", run { "android".substring(0, 4).uppercase() }, 0L, "\"android\".substring(0, 4).uppercase()")
        verify("unc", run { "functional".drop(1).take(3) }, 0L, "\"functional\".drop(1).take(3)")
        verify("edzc", run { "code".replace('o', 'z').reversed() }, 0L, "\"code\".replace('o', 'z').reversed()")
        verify("eulev", run { "value".replace('a', 'e').reversed() }, 0L, "\"value\".replace('a', 'e').reversed()")
        verify("un", run { "functional".drop(1).take(2) }, 0L, "\"functional\".drop(1).take(2)")
        verify("SE", run { "sequence".substring(0, 2).uppercase() }, 0L, "\"sequence\".substring(0, 2).uppercase()")
        verify("ctio", run { "functional".drop(3).take(4) }, 0L, "\"functional\".drop(3).take(4)")
        verify("SEQU", run { "sequence".substring(0, 4).uppercase() }, 0L, "\"sequence\".substring(0, 4).uppercase()")
        verify("ct", run { "functional".drop(3).take(2) }, 0L, "\"functional\".drop(3).take(2)")
        verify("COMP", run { "compose".substring(0, 4).uppercase() }, 0L, "\"compose\".substring(0, 4).uppercase()")
        verify("aulav", run { "value".replace('e', 'a').reversed() }, 0L, "\"value\".replace('e', 'a').reversed()")
        verify("euzav", run { "value".replace('l', 'z').reversed() }, 0L, "\"value\".replace('l', 'z').reversed()")
        verify("USPE", run { "suspend".substring(1, 5).uppercase() }, 0L, "\"suspend\".substring(1, 5).uppercase()")
        verify("ehlav", run { "value".replace('u', 'h').reversed() }, 0L, "\"value\".replace('u', 'h').reversed()")
        verify("gram", run { "programming".drop(3).take(4) }, 0L, "\"programming\".drop(3).take(4)")
        verify("LAM", run { "lambda".substring(0, 3).uppercase() }, 0L, "\"lambda\".substring(0, 3).uppercase()")
        verify("ro", run { "programming".drop(1).take(2) }, 0L, "\"programming\".drop(1).take(2)")
        verify("euzav", run { "value".replace('l', 'z').reversed() }, 0L, "\"value\".replace('l', 'z').reversed()")
        verify("eg", run { "delegate".drop(3).take(2) }, 0L, "\"delegate\".drop(3).take(2)")
        verify("AN", run { "channel".substring(2, 4).uppercase() }, 0L, "\"channel\".substring(2, 4).uppercase()")
        verify("CO", run { "coroutine".substring(0, 2).uppercase() }, 0L, "\"coroutine\".substring(0, 2).uppercase()")
        verify("edox", run { "code".replace('c', 'x').reversed() }, 0L, "\"code\".replace('c', 'x').reversed()")
    }

    @Test
    fun `verify string_operations batch 1`() {
        verify("atai", run { "data".replace('d', 'i').reversed() }, 0L, "\"data\".replace('d', 'i').reversed()")
        verify("SEQU", run { "sequence".substring(0, 4).uppercase() }, 0L, "\"sequence\".substring(0, 4).uppercase()")
        verify("dliow", run { "world".replace('r', 'i').reversed() }, 0L, "\"world\".replace('r', 'i').reversed()")
        verify("CO", run { "compose".substring(0, 2).uppercase() }, 0L, "\"compose\".substring(0, 2).uppercase()")
        verify("KOT", run { "kotlin".substring(0, 3).uppercase() }, 0L, "\"kotlin\".substring(0, 3).uppercase()")
        verify("MPOS", run { "compose".substring(2, 6).uppercase() }, 0L, "\"compose\".substring(2, 6).uppercase()")
        verify("osseh", run { "hello".replace('l', 's').reversed() }, 0L, "\"hello\".replace('l', 's').reversed()")
        verify("AMB", run { "lambda".substring(1, 4).uppercase() }, 0L, "\"lambda\".substring(1, 4).uppercase()")
        verify("eulev", run { "value".replace('a', 'e').reversed() }, 0L, "\"value\".replace('a', 'e').reversed()")
        verify("dlrod", run { "world".replace('w', 'd').reversed() }, 0L, "\"world\".replace('w', 'd').reversed()")
        verify("ctio", run { "functional".drop(3).take(4) }, 0L, "\"functional\".drop(3).take(4)")
        verify("HA", run { "channel".substring(1, 3).uppercase() }, 0L, "\"channel\".substring(1, 3).uppercase()")
        verify("eioc", run { "code".replace('d', 'i').reversed() }, 0L, "\"code\".replace('d', 'i').reversed()")
        verify("CH", run { "channel".substring(0, 2).uppercase() }, 0L, "\"channel\".substring(0, 2).uppercase()")
        verify("btbd", run { "data".replace('a', 'b').reversed() }, 0L, "\"data\".replace('a', 'b').reversed()")
        verify("wtwd", run { "data".replace('a', 'w').reversed() }, 0L, "\"data\".replace('a', 'w').reversed()")
        verify("muta", run { "immutable".drop(2).take(4) }, 0L, "\"immutable\".drop(2).take(4)")
        verify("OU", run { "coroutine".substring(3, 5).uppercase() }, 0L, "\"coroutine\".substring(3, 5).uppercase()")
        verify("ten", run { "extension".drop(2).take(3) }, 0L, "\"extension\".drop(2).take(3)")
        verify("gra", run { "programming".drop(3).take(3) }, 0L, "\"programming\".drop(3).take(3)")
        verify("MPO", run { "compose".substring(2, 5).uppercase() }, 0L, "\"compose\".substring(2, 5).uppercase()")
        verify("OTL", run { "kotlin".substring(1, 4).uppercase() }, 0L, "\"kotlin\".substring(1, 4).uppercase()")
        verify("rogr", run { "programming".drop(1).take(4) }, 0L, "\"programming\".drop(1).take(4)")
        verify("utab", run { "immutable".drop(3).take(4) }, 0L, "\"immutable\".drop(3).take(4)")
        verify("ten", run { "extension".drop(2).take(3) }, 0L, "\"extension\".drop(2).take(3)")
        verify("MBD", run { "lambda".substring(2, 5).uppercase() }, 0L, "\"lambda\".substring(2, 5).uppercase()")
        verify("AN", run { "channel".substring(2, 4).uppercase() }, 0L, "\"channel\".substring(2, 4).uppercase()")
        verify("NDR", run { "android".substring(1, 4).uppercase() }, 0L, "\"android\".substring(1, 4).uppercase()")
        verify("onneh", run { "hello".replace('l', 'n').reversed() }, 0L, "\"hello\".replace('l', 'n').reversed()")
        verify("ytyd", run { "data".replace('a', 'y').reversed() }, 0L, "\"data\".replace('a', 'y').reversed()")
        verify("ezlav", run { "value".replace('u', 'z').reversed() }, 0L, "\"value\".replace('u', 'z').reversed()")
        verify("idoc", run { "code".replace('e', 'i').reversed() }, 0L, "\"code\".replace('e', 'i').reversed()")
        verify("ogr", run { "programming".drop(2).take(3) }, 0L, "\"programming\".drop(2).take(3)")
        verify("ollek", run { "hello".replace('h', 'k').reversed() }, 0L, "\"hello\".replace('h', 'k').reversed()")
        verify("OMP", run { "compose".substring(1, 4).uppercase() }, 0L, "\"compose\".substring(1, 4).uppercase()")
        verify("le", run { "delegate".drop(2).take(2) }, 0L, "\"delegate\".drop(2).take(2)")
        verify("mmut", run { "immutable".drop(1).take(4) }, 0L, "\"immutable\".drop(1).take(4)")
        verify("TLI", run { "kotlin".substring(2, 5).uppercase() }, 0L, "\"kotlin\".substring(2, 5).uppercase()")
        verify("MBD", run { "lambda".substring(2, 5).uppercase() }, 0L, "\"lambda\".substring(2, 5).uppercase()")
        verify("ensi", run { "extension".drop(3).take(4) }, 0L, "\"extension\".drop(3).take(4)")
        verify("LAMB", run { "lambda".substring(0, 4).uppercase() }, 0L, "\"lambda\".substring(0, 4).uppercase()")
        verify("ewlav", run { "value".replace('u', 'w').reversed() }, 0L, "\"value\".replace('u', 'w').reversed()")
        verify("le", run { "delegate".drop(2).take(2) }, 0L, "\"delegate\".drop(2).take(2)")
        verify("NDRO", run { "android".substring(1, 5).uppercase() }, 0L, "\"android\".substring(1, 5).uppercase()")
        verify("rog", run { "programming".drop(1).take(3) }, 0L, "\"programming\".drop(1).take(3)")
        verify("mut", run { "immutable".drop(2).take(3) }, 0L, "\"immutable\".drop(2).take(3)")
        verify("qiltok", run { "kotlin".replace('n', 'q').reversed() }, 0L, "\"kotlin\".replace('n', 'q').reversed()")
        verify("unc", run { "functional".drop(1).take(3) }, 0L, "\"functional\".drop(1).take(3)")
        verify("SEQ", run { "sequence".substring(0, 3).uppercase() }, 0L, "\"sequence\".substring(0, 3).uppercase()")
        verify("dlrow", run { "world".replace('r', 'r').reversed() }, 0L, "\"world\".replace('r', 'r').reversed()")
    }

    @Test
    fun `verify scope_transform batch 0`() {
        verify("30", run { 11.let { it + 4 }.let { it * 2 } }, 0L, "11.let { it + 4 }.let { it * 2 }")
        verify("24", run { 7.let { it + 5 }.let { it * 2 } }, 0L, "7.let { it + 5 }.let { it * 2 }")
        verify("FT", run { "swift".run { drop(3).uppercase() } }, 0L, "\"swift\".run { drop(3).uppercase() }")
        verify("44", run { 14.let { it + 8 }.let { it * 2 } }, 0L, "14.let { it + 8 }.let { it * 2 }")
        verify("38", run { 12.let { it + 7 }.let { it * 2 } }, 0L, "12.let { it + 7 }.let { it * 2 }")
        verify("8", run { 1.let { it + 8 }.run { this - 1 } }, 0L, "1.let { it + 8 }.run { this - 1 }")
        verify("52", run { 8.let { it + 5 }.let { it * 4 } }, 0L, "8.let { it + 5 }.let { it * 4 }")
        verify("7", run { 2.let { it + 7 }.run { this - 2 } }, 0L, "2.let { it + 7 }.run { this - 2 }")
        verify("VA", run { "java".run { drop(2).uppercase() } }, 0L, "\"java\".run { drop(2).uppercase() }")
        verify("UST", run { "rust".run { drop(1).uppercase() } }, 0L, "\"rust\".run { drop(1).uppercase() }")
        verify("18", run { 5.let { it + 1 }.let { it * 3 } }, 0L, "5.let { it + 1 }.let { it * 3 }")
        verify("13", run { 11.let { it + 4 }.run { this - 2 } }, 0L, "11.let { it + 4 }.run { this - 2 }")
        verify("VA", run { "java".run { drop(2).uppercase() } }, 0L, "\"java\".run { drop(2).uppercase() }")
        verify("ST", run { "rust".run { drop(2).uppercase() } }, 0L, "\"rust\".run { drop(2).uppercase() }")
        verify("IFT", run { "swift".run { drop(2).uppercase() } }, 0L, "\"swift\".run { drop(2).uppercase() }")
        verify("LIN", run { "kotlin".run { drop(3).uppercase() } }, 0L, "\"kotlin\".run { drop(3).uppercase() }")
        verify("84", run { 19.let { it + 2 }.let { it * 4 } }, 0L, "19.let { it + 2 }.let { it * 4 }")
        verify("13", run { 14.let { it + 2 }.run { this - 3 } }, 0L, "14.let { it + 2 }.run { this - 3 }")
        verify("OTLIN", run { "kotlin".run { drop(1).uppercase() } }, 0L, "\"kotlin\".run { drop(1).uppercase() }")
        verify("LIN", run { "kotlin".run { drop(3).uppercase() } }, 0L, "\"kotlin\".run { drop(3).uppercase() }")
        verify("15", run { 3.let { it + 2 }.let { it * 3 } }, 0L, "3.let { it + 2 }.let { it * 3 }")
        verify("52", run { 6.let { it + 7 }.let { it * 4 } }, 0L, "6.let { it + 7 }.let { it * 4 }")
        verify("13", run { 9.let { it + 6 }.run { this - 2 } }, 0L, "9.let { it + 6 }.run { this - 2 }")
        verify("18", run { 1.let { it + 5 }.let { it * 3 } }, 0L, "1.let { it + 5 }.let { it * 3 }")
        verify("20", run { 2.let { it + 8 }.let { it * 2 } }, 0L, "2.let { it + 8 }.let { it * 2 }")
        verify("36", run { 11.let { it + 1 }.let { it * 3 } }, 0L, "11.let { it + 1 }.let { it * 3 }")
        verify("11", run { 4.let { it + 9 }.run { this - 2 } }, 0L, "4.let { it + 9 }.run { this - 2 }")
        verify("56", run { 7.let { it + 7 }.let { it * 4 } }, 0L, "7.let { it + 7 }.let { it * 4 }")
        verify("36", run { 8.let { it + 1 }.let { it * 4 } }, 0L, "8.let { it + 1 }.let { it * 4 }")
        verify("11", run { 7.let { it + 7 }.run { this - 3 } }, 0L, "7.let { it + 7 }.run { this - 3 }")
        verify("ALA", run { "scala".run { drop(2).uppercase() } }, 0L, "\"scala\".run { drop(2).uppercase() }")
        verify("ST", run { "rust".run { drop(2).uppercase() } }, 0L, "\"rust\".run { drop(2).uppercase() }")
        verify("12", run { 9.let { it + 4 }.run { this - 1 } }, 0L, "9.let { it + 4 }.run { this - 1 }")
        verify("18", run { 1.let { it + 8 }.let { it * 2 } }, 0L, "1.let { it + 8 }.let { it * 2 }")
        verify("12", run { 8.let { it + 6 }.run { this - 2 } }, 0L, "8.let { it + 6 }.run { this - 2 }")
        verify("60", run { 14.let { it + 1 }.let { it * 4 } }, 0L, "14.let { it + 1 }.let { it * 4 }")
        verify("7", run { 1.let { it + 9 }.run { this - 3 } }, 0L, "1.let { it + 9 }.run { this - 3 }")
        verify("44", run { 10.let { it + 1 }.let { it * 4 } }, 0L, "10.let { it + 1 }.let { it * 4 }")
        verify("LIN", run { "kotlin".run { drop(3).uppercase() } }, 0L, "\"kotlin\".run { drop(3).uppercase() }")
        verify("A", run { "java".run { drop(3).uppercase() } }, 0L, "\"java\".run { drop(3).uppercase() }")
        verify("40", run { 5.let { it + 5 }.let { it * 4 } }, 0L, "5.let { it + 5 }.let { it * 4 }")
        verify("FT", run { "swift".run { drop(3).uppercase() } }, 0L, "\"swift\".run { drop(3).uppercase() }")
        verify("9", run { 7.let { it + 6 }.run { this - 4 } }, 0L, "7.let { it + 6 }.run { this - 4 }")
        verify("51", run { 10.let { it + 7 }.let { it * 3 } }, 0L, "10.let { it + 7 }.let { it * 3 }")
        verify("14", run { 14.let { it + 1 }.run { this - 1 } }, 0L, "14.let { it + 1 }.run { this - 1 }")
        verify("ALA", run { "scala".run { drop(2).uppercase() } }, 0L, "\"scala\".run { drop(2).uppercase() }")
        verify("8", run { 7.let { it + 3 }.run { this - 2 } }, 0L, "7.let { it + 3 }.run { this - 2 }")
        verify("24", run { 6.let { it + 6 }.let { it * 2 } }, 0L, "6.let { it + 6 }.let { it * 2 }")
        verify("24", run { 5.let { it + 7 }.let { it * 2 } }, 0L, "5.let { it + 7 }.let { it * 2 }")
        verify("TLIN", run { "kotlin".run { drop(2).uppercase() } }, 0L, "\"kotlin\".run { drop(2).uppercase() }")
    }

    @Test
    fun `verify scope_transform batch 1`() {
        verify("AVA", run { "java".run { drop(1).uppercase() } }, 0L, "\"java\".run { drop(1).uppercase() }")
        verify("24", run { 4.let { it + 2 }.let { it * 4 } }, 0L, "4.let { it + 2 }.let { it * 4 }")
        verify("IFT", run { "swift".run { drop(2).uppercase() } }, 0L, "\"swift\".run { drop(2).uppercase() }")
        verify("52", run { 19.let { it + 7 }.let { it * 2 } }, 0L, "19.let { it + 7 }.let { it * 2 }")
        verify("51", run { 10.let { it + 7 }.let { it * 3 } }, 0L, "10.let { it + 7 }.let { it * 3 }")
        verify("60", run { 6.let { it + 9 }.let { it * 4 } }, 0L, "6.let { it + 9 }.let { it * 4 }")
        verify("CALA", run { "scala".run { drop(1).uppercase() } }, 0L, "\"scala\".run { drop(1).uppercase() }")
        verify("60", run { 15.let { it + 5 }.let { it * 3 } }, 0L, "15.let { it + 5 }.let { it * 3 }")
        verify("OTLIN", run { "kotlin".run { drop(1).uppercase() } }, 0L, "\"kotlin\".run { drop(1).uppercase() }")
        verify("TLIN", run { "kotlin".run { drop(2).uppercase() } }, 0L, "\"kotlin\".run { drop(2).uppercase() }")
        verify("8", run { 3.let { it + 6 }.run { this - 1 } }, 0L, "3.let { it + 6 }.run { this - 1 }")
        verify("44", run { 14.let { it + 8 }.let { it * 2 } }, 0L, "14.let { it + 8 }.let { it * 2 }")
        verify("VA", run { "java".run { drop(2).uppercase() } }, 0L, "\"java\".run { drop(2).uppercase() }")
        verify("46", run { 19.let { it + 4 }.let { it * 2 } }, 0L, "19.let { it + 4 }.let { it * 2 }")
        verify("VA", run { "java".run { drop(2).uppercase() } }, 0L, "\"java\".run { drop(2).uppercase() }")
        verify("OTLIN", run { "kotlin".run { drop(1).uppercase() } }, 0L, "\"kotlin\".run { drop(1).uppercase() }")
        verify("9", run { 6.let { it + 5 }.run { this - 2 } }, 0L, "6.let { it + 5 }.run { this - 2 }")
        verify("14", run { 6.let { it + 1 }.let { it * 2 } }, 0L, "6.let { it + 1 }.let { it * 2 }")
        verify("12", run { 13.let { it + 2 }.run { this - 3 } }, 0L, "13.let { it + 2 }.run { this - 3 }")
        verify("7", run { 1.let { it + 9 }.run { this - 3 } }, 0L, "1.let { it + 9 }.run { this - 3 }")
        verify("27", run { 3.let { it + 6 }.let { it * 3 } }, 0L, "3.let { it + 6 }.let { it * 3 }")
        verify("39", run { 8.let { it + 5 }.let { it * 3 } }, 0L, "8.let { it + 5 }.let { it * 3 }")
        verify("9", run { 8.let { it + 4 }.run { this - 3 } }, 0L, "8.let { it + 4 }.run { this - 3 }")
        verify("9", run { 6.let { it + 6 }.run { this - 3 } }, 0L, "6.let { it + 6 }.run { this - 3 }")
        verify("13", run { 9.let { it + 8 }.run { this - 4 } }, 0L, "9.let { it + 8 }.run { this - 4 }")
        verify("108", run { 18.let { it + 9 }.let { it * 4 } }, 0L, "18.let { it + 9 }.let { it * 4 }")
        verify("28", run { 8.let { it + 6 }.let { it * 2 } }, 0L, "8.let { it + 6 }.let { it * 2 }")
        verify("30", run { 8.let { it + 2 }.let { it * 3 } }, 0L, "8.let { it + 2 }.let { it * 3 }")
        verify("T", run { "rust".run { drop(3).uppercase() } }, 0L, "\"rust\".run { drop(3).uppercase() }")
        verify("OTLIN", run { "kotlin".run { drop(1).uppercase() } }, 0L, "\"kotlin\".run { drop(1).uppercase() }")
        verify("CALA", run { "scala".run { drop(1).uppercase() } }, 0L, "\"scala\".run { drop(1).uppercase() }")
        verify("UST", run { "rust".run { drop(1).uppercase() } }, 0L, "\"rust\".run { drop(1).uppercase() }")
        verify("4", run { 1.let { it + 5 }.run { this - 2 } }, 0L, "1.let { it + 5 }.run { this - 2 }")
        verify("IFT", run { "swift".run { drop(2).uppercase() } }, 0L, "\"swift\".run { drop(2).uppercase() }")
        verify("12", run { 2.let { it + 2 }.let { it * 3 } }, 0L, "2.let { it + 2 }.let { it * 3 }")
        verify("7", run { 2.let { it + 8 }.run { this - 3 } }, 0L, "2.let { it + 8 }.run { this - 3 }")
        verify("5", run { 5.let { it + 4 }.run { this - 4 } }, 0L, "5.let { it + 4 }.run { this - 4 }")
        verify("10", run { 2.let { it + 3 }.let { it * 2 } }, 0L, "2.let { it + 3 }.let { it * 2 }")
        verify("56", run { 5.let { it + 9 }.let { it * 4 } }, 0L, "5.let { it + 9 }.let { it * 4 }")
        verify("7", run { 7.let { it + 3 }.run { this - 3 } }, 0L, "7.let { it + 3 }.run { this - 3 }")
        verify("64", run { 15.let { it + 1 }.let { it * 4 } }, 0L, "15.let { it + 1 }.let { it * 4 }")
        verify("LA", run { "scala".run { drop(3).uppercase() } }, 0L, "\"scala\".run { drop(3).uppercase() }")
        verify("7", run { 3.let { it + 5 }.run { this - 1 } }, 0L, "3.let { it + 5 }.run { this - 1 }")
        verify("104", run { 18.let { it + 8 }.let { it * 4 } }, 0L, "18.let { it + 8 }.let { it * 4 }")
        verify("8", run { 6.let { it + 4 }.run { this - 2 } }, 0L, "6.let { it + 4 }.run { this - 2 }")
        verify("6", run { 6.let { it + 2 }.run { this - 2 } }, 0L, "6.let { it + 2 }.run { this - 2 }")
        verify("A", run { "java".run { drop(3).uppercase() } }, 0L, "\"java\".run { drop(3).uppercase() }")
        verify("6", run { 5.let { it + 4 }.run { this - 3 } }, 0L, "5.let { it + 4 }.run { this - 3 }")
        verify("21", run { 1.let { it + 6 }.let { it * 3 } }, 0L, "1.let { it + 6 }.let { it * 3 }")
        verify("AVA", run { "java".run { drop(1).uppercase() } }, 0L, "\"java\".run { drop(1).uppercase() }")
    }

    @Test
    fun `verify map_operations batch 0`() {
        verify("[]", run { mapOf("a" to 14, "b" to 11, "c" to 17).filter { it.value > 17 }.keys.sorted() }, 0L, "mapOf(\"a\" to 14, \"b\" to 11, \"c\" to 17).filter { it.value > 17 }.keys.sorted()")
        verify("[a, b, c]", run { mapOf("a" to 14, "b" to 17, "c" to 19, "d" to 4).filter { it.value > 4 }.keys.sorted() }, 0L, "mapOf(\"a\" to 14, \"b\" to 17, \"c\" to 19, \"d\" to 4).filter { it.value > 4 }.keys.sorted()")
        verify("76", run { mapOf("x" to 7, "y" to 6, "z" to 6).mapValues { it.value * 4 }.values.sum() }, 0L, "mapOf(\"x\" to 7, \"y\" to 6, \"z\" to 6).mapValues { it.value * 4 }.values.sum()")
        verify("[b, c]", run { mapOf("a" to 11, "b" to 13, "c" to 17, "d" to 11).filter { it.value > 11 }.keys.sorted() }, 0L, "mapOf(\"a\" to 11, \"b\" to 13, \"c\" to 17, \"d\" to 11).filter { it.value > 11 }.keys.sorted()")
        verify("[b, c]", run { mapOf("a" to 2, "b" to 6, "c" to 19).filter { it.value > 2 }.keys.sorted() }, 0L, "mapOf(\"a\" to 2, \"b\" to 6, \"c\" to 19).filter { it.value > 2 }.keys.sorted()")
        verify("4", run { mapOf("a" to 8, "b" to 4, "c" to 12).getOrDefault("b", 96) }, 0L, "mapOf(\"a\" to 8, \"b\" to 4, \"c\" to 12).getOrDefault(\"b\", 96)")
        verify("[a]", run { mapOf("a" to 13, "b" to 4, "c" to 9).filter { it.value > 9 }.keys.sorted() }, 0L, "mapOf(\"a\" to 13, \"b\" to 4, \"c\" to 9).filter { it.value > 9 }.keys.sorted()")
        verify("12", run { mapOf("a" to 18, "b" to 19, "c" to 12).getOrDefault("c", 60) }, 0L, "mapOf(\"a\" to 18, \"b\" to 19, \"c\" to 12).getOrDefault(\"c\", 60)")
        verify("48", run { mapOf("x" to 4, "y" to 2, "z" to 6).mapValues { it.value * 4 }.values.sum() }, 0L, "mapOf(\"x\" to 4, \"y\" to 2, \"z\" to 6).mapValues { it.value * 4 }.values.sum()")
        verify("66", run { mapOf("x" to 6, "y" to 7, "z" to 9).mapValues { it.value * 3 }.values.sum() }, 0L, "mapOf(\"x\" to 6, \"y\" to 7, \"z\" to 9).mapValues { it.value * 3 }.values.sum()")
        verify("[]", run { mapOf("a" to 13, "b" to 10, "c" to 17).filter { it.value > 17 }.keys.sorted() }, 0L, "mapOf(\"a\" to 13, \"b\" to 10, \"c\" to 17).filter { it.value > 17 }.keys.sorted()")
        verify("55", run { mapOf("a" to 15, "b" to 3, "c" to 15).getOrDefault("e", 55) }, 0L, "mapOf(\"a\" to 15, \"b\" to 3, \"c\" to 15).getOrDefault(\"e\", 55)")
        verify("51", run { mapOf("x" to 4, "y" to 5, "z" to 8).mapValues { it.value * 3 }.values.sum() }, 0L, "mapOf(\"x\" to 4, \"y\" to 5, \"z\" to 8).mapValues { it.value * 3 }.values.sum()")
        verify("16", run { mapOf("x" to 1, "y" to 2, "z" to 1).mapValues { it.value * 4 }.values.sum() }, 0L, "mapOf(\"x\" to 1, \"y\" to 2, \"z\" to 1).mapValues { it.value * 4 }.values.sum()")
        verify("33", run { mapOf("x" to 1, "y" to 8, "z" to 2).mapValues { it.value * 3 }.values.sum() }, 0L, "mapOf(\"x\" to 1, \"y\" to 8, \"z\" to 2).mapValues { it.value * 3 }.values.sum()")
        verify("14", run { mapOf("x" to 3, "y" to 3, "z" to 1).mapValues { it.value * 2 }.values.sum() }, 0L, "mapOf(\"x\" to 3, \"y\" to 3, \"z\" to 1).mapValues { it.value * 2 }.values.sum()")
        verify("[]", run { mapOf("a" to 10, "b" to 7, "c" to 8).filter { it.value > 10 }.keys.sorted() }, 0L, "mapOf(\"a\" to 10, \"b\" to 7, \"c\" to 8).filter { it.value > 10 }.keys.sorted()")
        verify("16", run { mapOf("a" to 19, "b" to 16, "c" to 14).getOrDefault("b", 53) }, 0L, "mapOf(\"a\" to 19, \"b\" to 16, \"c\" to 14).getOrDefault(\"b\", 53)")
        verify("72", run { mapOf("x" to 8, "y" to 7, "z" to 3).mapValues { it.value * 4 }.values.sum() }, 0L, "mapOf(\"x\" to 8, \"y\" to 7, \"z\" to 3).mapValues { it.value * 4 }.values.sum()")
        verify("42", run { mapOf("x" to 5, "y" to 9, "z" to 7).mapValues { it.value * 2 }.values.sum() }, 0L, "mapOf(\"x\" to 5, \"y\" to 9, \"z\" to 7).mapValues { it.value * 2 }.values.sum()")
        verify("[]", run { mapOf("a" to 4, "b" to 10, "c" to 19).filter { it.value > 19 }.keys.sorted() }, 0L, "mapOf(\"a\" to 4, \"b\" to 10, \"c\" to 19).filter { it.value > 19 }.keys.sorted()")
        verify("[a]", run { mapOf("a" to 19, "b" to 4, "c" to 1, "d" to 5).filter { it.value > 5 }.keys.sorted() }, 0L, "mapOf(\"a\" to 19, \"b\" to 4, \"c\" to 1, \"d\" to 5).filter { it.value > 5 }.keys.sorted()")
        verify("66", run { mapOf("a" to 19, "b" to 10, "c" to 11).getOrDefault("d", 66) }, 0L, "mapOf(\"a\" to 19, \"b\" to 10, \"c\" to 11).getOrDefault(\"d\", 66)")
        verify("[b, c]", run { mapOf("a" to 6, "b" to 14, "c" to 11, "d" to 8).filter { it.value > 8 }.keys.sorted() }, 0L, "mapOf(\"a\" to 6, \"b\" to 14, \"c\" to 11, \"d\" to 8).filter { it.value > 8 }.keys.sorted()")
        verify("[a]", run { mapOf("a" to 10, "b" to 1, "c" to 8).filter { it.value > 8 }.keys.sorted() }, 0L, "mapOf(\"a\" to 10, \"b\" to 1, \"c\" to 8).filter { it.value > 8 }.keys.sorted()")
        verify("[]", run { mapOf("a" to 5, "b" to 2, "c" to 18, "d" to 11).filter { it.value > 18 }.keys.sorted() }, 0L, "mapOf(\"a\" to 5, \"b\" to 2, \"c\" to 18, \"d\" to 11).filter { it.value > 18 }.keys.sorted()")
        verify("87", run { mapOf("a" to 4, "b" to 1, "c" to 3).getOrDefault("d", 87) }, 0L, "mapOf(\"a\" to 4, \"b\" to 1, \"c\" to 3).getOrDefault(\"d\", 87)")
        verify("[]", run { mapOf("a" to 11, "b" to 14, "c" to 16, "d" to 9).filter { it.value > 16 }.keys.sorted() }, 0L, "mapOf(\"a\" to 11, \"b\" to 14, \"c\" to 16, \"d\" to 9).filter { it.value > 16 }.keys.sorted()")
        verify("[a, c]", run { mapOf("a" to 7, "b" to 1, "c" to 10).filter { it.value > 1 }.keys.sorted() }, 0L, "mapOf(\"a\" to 7, \"b\" to 1, \"c\" to 10).filter { it.value > 1 }.keys.sorted()")
        verify("72", run { mapOf("a" to 11, "b" to 8, "c" to 17).getOrDefault("e", 72) }, 0L, "mapOf(\"a\" to 11, \"b\" to 8, \"c\" to 17).getOrDefault(\"e\", 72)")
        verify("22", run { mapOf("x" to 4, "y" to 2, "z" to 5).mapValues { it.value * 2 }.values.sum() }, 0L, "mapOf(\"x\" to 4, \"y\" to 2, \"z\" to 5).mapValues { it.value * 2 }.values.sum()")
        verify("38", run { mapOf("x" to 9, "y" to 5, "z" to 5).mapValues { it.value * 2 }.values.sum() }, 0L, "mapOf(\"x\" to 9, \"y\" to 5, \"z\" to 5).mapValues { it.value * 2 }.values.sum()")
        verify("60", run { mapOf("a" to 9, "b" to 15, "c" to 19).getOrDefault("e", 60) }, 0L, "mapOf(\"a\" to 9, \"b\" to 15, \"c\" to 19).getOrDefault(\"e\", 60)")
        verify("[c]", run { mapOf("a" to 4, "b" to 6, "c" to 19, "d" to 4).filter { it.value > 6 }.keys.sorted() }, 0L, "mapOf(\"a\" to 4, \"b\" to 6, \"c\" to 19, \"d\" to 4).filter { it.value > 6 }.keys.sorted()")
        verify("2", run { mapOf("a" to 2, "b" to 3, "c" to 13).getOrDefault("a", 98) }, 0L, "mapOf(\"a\" to 2, \"b\" to 3, \"c\" to 13).getOrDefault(\"a\", 98)")
        verify("[]", run { mapOf("a" to 12, "b" to 12, "c" to 6, "d" to 2).filter { it.value > 12 }.keys.sorted() }, 0L, "mapOf(\"a\" to 12, \"b\" to 12, \"c\" to 6, \"d\" to 2).filter { it.value > 12 }.keys.sorted()")
        verify("12", run { mapOf("a" to 17, "b" to 12, "c" to 14).getOrDefault("b", 51) }, 0L, "mapOf(\"a\" to 17, \"b\" to 12, \"c\" to 14).getOrDefault(\"b\", 51)")
        verify("[a, c, d]", run { mapOf("a" to 6, "b" to 3, "c" to 7, "d" to 14).filter { it.value > 3 }.keys.sorted() }, 0L, "mapOf(\"a\" to 6, \"b\" to 3, \"c\" to 7, \"d\" to 14).filter { it.value > 3 }.keys.sorted()")
        verify("48", run { mapOf("x" to 6, "y" to 9, "z" to 9).mapValues { it.value * 2 }.values.sum() }, 0L, "mapOf(\"x\" to 6, \"y\" to 9, \"z\" to 9).mapValues { it.value * 2 }.values.sum()")
        verify("52", run { mapOf("x" to 3, "y" to 9, "z" to 1).mapValues { it.value * 4 }.values.sum() }, 0L, "mapOf(\"x\" to 3, \"y\" to 9, \"z\" to 1).mapValues { it.value * 4 }.values.sum()")
        verify("[c]", run { mapOf("a" to 6, "b" to 2, "c" to 12).filter { it.value > 6 }.keys.sorted() }, 0L, "mapOf(\"a\" to 6, \"b\" to 2, \"c\" to 12).filter { it.value > 6 }.keys.sorted()")
        verify("84", run { mapOf("x" to 3, "y" to 9, "z" to 9).mapValues { it.value * 4 }.values.sum() }, 0L, "mapOf(\"x\" to 3, \"y\" to 9, \"z\" to 9).mapValues { it.value * 4 }.values.sum()")
        verify("16", run { mapOf("a" to 8, "b" to 16, "c" to 3).getOrDefault("b", 82) }, 0L, "mapOf(\"a\" to 8, \"b\" to 16, \"c\" to 3).getOrDefault(\"b\", 82)")
        verify("[b]", run { mapOf("a" to 5, "b" to 9, "c" to 4, "d" to 1).filter { it.value > 5 }.keys.sorted() }, 0L, "mapOf(\"a\" to 5, \"b\" to 9, \"c\" to 4, \"d\" to 1).filter { it.value > 5 }.keys.sorted()")
        verify("95", run { mapOf("a" to 6, "b" to 13, "c" to 16).getOrDefault("d", 95) }, 0L, "mapOf(\"a\" to 6, \"b\" to 13, \"c\" to 16).getOrDefault(\"d\", 95)")
        verify("38", run { mapOf("x" to 9, "y" to 8, "z" to 2).mapValues { it.value * 2 }.values.sum() }, 0L, "mapOf(\"x\" to 9, \"y\" to 8, \"z\" to 2).mapValues { it.value * 2 }.values.sum()")
        verify("87", run { mapOf("a" to 1, "b" to 12, "c" to 8).getOrDefault("d", 87) }, 0L, "mapOf(\"a\" to 1, \"b\" to 12, \"c\" to 8).getOrDefault(\"d\", 87)")
        verify("[b, d]", run { mapOf("a" to 8, "b" to 19, "c" to 3, "d" to 16).filter { it.value > 8 }.keys.sorted() }, 0L, "mapOf(\"a\" to 8, \"b\" to 19, \"c\" to 3, \"d\" to 16).filter { it.value > 8 }.keys.sorted()")
        verify("[a, c]", run { mapOf("a" to 17, "b" to 5, "c" to 17).filter { it.value > 5 }.keys.sorted() }, 0L, "mapOf(\"a\" to 17, \"b\" to 5, \"c\" to 17).filter { it.value > 5 }.keys.sorted()")
        verify("24", run { mapOf("x" to 1, "y" to 5, "z" to 6).mapValues { it.value * 2 }.values.sum() }, 0L, "mapOf(\"x\" to 1, \"y\" to 5, \"z\" to 6).mapValues { it.value * 2 }.values.sum()")
    }

    @Test
    fun `verify map_operations batch 1`() {
        verify("80", run { mapOf("x" to 5, "y" to 7, "z" to 8).mapValues { it.value * 4 }.values.sum() }, 0L, "mapOf(\"x\" to 5, \"y\" to 7, \"z\" to 8).mapValues { it.value * 4 }.values.sum()")
        verify("[]", run { mapOf("a" to 13, "b" to 18, "c" to 3, "d" to 11).filter { it.value > 18 }.keys.sorted() }, 0L, "mapOf(\"a\" to 13, \"b\" to 18, \"c\" to 3, \"d\" to 11).filter { it.value > 18 }.keys.sorted()")
        verify("39", run { mapOf("x" to 2, "y" to 2, "z" to 9).mapValues { it.value * 3 }.values.sum() }, 0L, "mapOf(\"x\" to 2, \"y\" to 2, \"z\" to 9).mapValues { it.value * 3 }.values.sum()")
        verify("[a, c, d]", run { mapOf("a" to 16, "b" to 4, "c" to 5, "d" to 5).filter { it.value > 4 }.keys.sorted() }, 0L, "mapOf(\"a\" to 16, \"b\" to 4, \"c\" to 5, \"d\" to 5).filter { it.value > 4 }.keys.sorted()")
        verify("[]", run { mapOf("a" to 17, "b" to 10, "c" to 10).filter { it.value > 17 }.keys.sorted() }, 0L, "mapOf(\"a\" to 17, \"b\" to 10, \"c\" to 10).filter { it.value > 17 }.keys.sorted()")
        verify("[c]", run { mapOf("a" to 6, "b" to 7, "c" to 18, "d" to 1).filter { it.value > 7 }.keys.sorted() }, 0L, "mapOf(\"a\" to 6, \"b\" to 7, \"c\" to 18, \"d\" to 1).filter { it.value > 7 }.keys.sorted()")
        verify("36", run { mapOf("x" to 7, "y" to 4, "z" to 1).mapValues { it.value * 3 }.values.sum() }, 0L, "mapOf(\"x\" to 7, \"y\" to 4, \"z\" to 1).mapValues { it.value * 3 }.values.sum()")
        verify("[c]", run { mapOf("a" to 13, "b" to 3, "c" to 19, "d" to 2).filter { it.value > 13 }.keys.sorted() }, 0L, "mapOf(\"a\" to 13, \"b\" to 3, \"c\" to 19, \"d\" to 2).filter { it.value > 13 }.keys.sorted()")
        verify("50", run { mapOf("x" to 9, "y" to 7, "z" to 9).mapValues { it.value * 2 }.values.sum() }, 0L, "mapOf(\"x\" to 9, \"y\" to 7, \"z\" to 9).mapValues { it.value * 2 }.values.sum()")
        verify("36", run { mapOf("x" to 2, "y" to 8, "z" to 2).mapValues { it.value * 3 }.values.sum() }, 0L, "mapOf(\"x\" to 2, \"y\" to 8, \"z\" to 2).mapValues { it.value * 3 }.values.sum()")
        verify("79", run { mapOf("a" to 10, "b" to 5, "c" to 5).getOrDefault("e", 79) }, 0L, "mapOf(\"a\" to 10, \"b\" to 5, \"c\" to 5).getOrDefault(\"e\", 79)")
        verify("[b, d]", run { mapOf("a" to 6, "b" to 9, "c" to 4, "d" to 18).filter { it.value > 6 }.keys.sorted() }, 0L, "mapOf(\"a\" to 6, \"b\" to 9, \"c\" to 4, \"d\" to 18).filter { it.value > 6 }.keys.sorted()")
        verify("18", run { mapOf("x" to 4, "y" to 2, "z" to 3).mapValues { it.value * 2 }.values.sum() }, 0L, "mapOf(\"x\" to 4, \"y\" to 2, \"z\" to 3).mapValues { it.value * 2 }.values.sum()")
        verify("[b, c]", run { mapOf("a" to 3, "b" to 8, "c" to 13, "d" to 3).filter { it.value > 3 }.keys.sorted() }, 0L, "mapOf(\"a\" to 3, \"b\" to 8, \"c\" to 13, \"d\" to 3).filter { it.value > 3 }.keys.sorted()")
        verify("16", run { mapOf("x" to 2, "y" to 5, "z" to 1).mapValues { it.value * 2 }.values.sum() }, 0L, "mapOf(\"x\" to 2, \"y\" to 5, \"z\" to 1).mapValues { it.value * 2 }.values.sum()")
        verify("40", run { mapOf("x" to 5, "y" to 7, "z" to 8).mapValues { it.value * 2 }.values.sum() }, 0L, "mapOf(\"x\" to 5, \"y\" to 7, \"z\" to 8).mapValues { it.value * 2 }.values.sum()")
        verify("16", run { mapOf("a" to 11, "b" to 16, "c" to 16).getOrDefault("b", 81) }, 0L, "mapOf(\"a\" to 11, \"b\" to 16, \"c\" to 16).getOrDefault(\"b\", 81)")
        verify("[b]", run { mapOf("a" to 13, "b" to 19, "c" to 8).filter { it.value > 13 }.keys.sorted() }, 0L, "mapOf(\"a\" to 13, \"b\" to 19, \"c\" to 8).filter { it.value > 13 }.keys.sorted()")
        verify("8", run { mapOf("a" to 4, "b" to 2, "c" to 8).getOrDefault("c", 78) }, 0L, "mapOf(\"a\" to 4, \"b\" to 2, \"c\" to 8).getOrDefault(\"c\", 78)")
        verify("5", run { mapOf("a" to 6, "b" to 10, "c" to 5).getOrDefault("c", 96) }, 0L, "mapOf(\"a\" to 6, \"b\" to 10, \"c\" to 5).getOrDefault(\"c\", 96)")
        verify("[a, c]", run { mapOf("a" to 15, "b" to 4, "c" to 19, "d" to 11).filter { it.value > 11 }.keys.sorted() }, 0L, "mapOf(\"a\" to 15, \"b\" to 4, \"c\" to 19, \"d\" to 11).filter { it.value > 11 }.keys.sorted()")
        verify("[]", run { mapOf("a" to 1, "b" to 1, "c" to 7).filter { it.value > 7 }.keys.sorted() }, 0L, "mapOf(\"a\" to 1, \"b\" to 1, \"c\" to 7).filter { it.value > 7 }.keys.sorted()")
        verify("61", run { mapOf("a" to 6, "b" to 7, "c" to 2).getOrDefault("d", 61) }, 0L, "mapOf(\"a\" to 6, \"b\" to 7, \"c\" to 2).getOrDefault(\"d\", 61)")
        verify("75", run { mapOf("a" to 10, "b" to 9, "c" to 1).getOrDefault("d", 75) }, 0L, "mapOf(\"a\" to 10, \"b\" to 9, \"c\" to 1).getOrDefault(\"d\", 75)")
        verify("97", run { mapOf("a" to 6, "b" to 13, "c" to 9).getOrDefault("d", 97) }, 0L, "mapOf(\"a\" to 6, \"b\" to 13, \"c\" to 9).getOrDefault(\"d\", 97)")
        verify("[b, c, d]", run { mapOf("a" to 6, "b" to 8, "c" to 17, "d" to 18).filter { it.value > 6 }.keys.sorted() }, 0L, "mapOf(\"a\" to 6, \"b\" to 8, \"c\" to 17, \"d\" to 18).filter { it.value > 6 }.keys.sorted()")
        verify("[a, b, c]", run { mapOf("a" to 9, "b" to 8, "c" to 9, "d" to 6).filter { it.value > 6 }.keys.sorted() }, 0L, "mapOf(\"a\" to 9, \"b\" to 8, \"c\" to 9, \"d\" to 6).filter { it.value > 6 }.keys.sorted()")
        verify("[]", run { mapOf("a" to 12, "b" to 6, "c" to 16).filter { it.value > 16 }.keys.sorted() }, 0L, "mapOf(\"a\" to 12, \"b\" to 6, \"c\" to 16).filter { it.value > 16 }.keys.sorted()")
        verify("44", run { mapOf("x" to 4, "y" to 6, "z" to 1).mapValues { it.value * 4 }.values.sum() }, 0L, "mapOf(\"x\" to 4, \"y\" to 6, \"z\" to 1).mapValues { it.value * 4 }.values.sum()")
        verify("12", run { mapOf("x" to 2, "y" to 1, "z" to 1).mapValues { it.value * 3 }.values.sum() }, 0L, "mapOf(\"x\" to 2, \"y\" to 1, \"z\" to 1).mapValues { it.value * 3 }.values.sum()")
        verify("72", run { mapOf("x" to 9, "y" to 4, "z" to 5).mapValues { it.value * 4 }.values.sum() }, 0L, "mapOf(\"x\" to 9, \"y\" to 4, \"z\" to 5).mapValues { it.value * 4 }.values.sum()")
        verify("52", run { mapOf("x" to 7, "y" to 1, "z" to 5).mapValues { it.value * 4 }.values.sum() }, 0L, "mapOf(\"x\" to 7, \"y\" to 1, \"z\" to 5).mapValues { it.value * 4 }.values.sum()")
        verify("9", run { mapOf("a" to 18, "b" to 9, "c" to 12).getOrDefault("b", 89) }, 0L, "mapOf(\"a\" to 18, \"b\" to 9, \"c\" to 12).getOrDefault(\"b\", 89)")
        verify("36", run { mapOf("x" to 1, "y" to 5, "z" to 3).mapValues { it.value * 4 }.values.sum() }, 0L, "mapOf(\"x\" to 1, \"y\" to 5, \"z\" to 3).mapValues { it.value * 4 }.values.sum()")
        verify("[b]", run { mapOf("a" to 4, "b" to 15, "c" to 14, "d" to 7).filter { it.value > 14 }.keys.sorted() }, 0L, "mapOf(\"a\" to 4, \"b\" to 15, \"c\" to 14, \"d\" to 7).filter { it.value > 14 }.keys.sorted()")
        verify("92", run { mapOf("a" to 10, "b" to 11, "c" to 7).getOrDefault("d", 92) }, 0L, "mapOf(\"a\" to 10, \"b\" to 11, \"c\" to 7).getOrDefault(\"d\", 92)")
        verify("17", run { mapOf("a" to 17, "b" to 16, "c" to 8).getOrDefault("a", 90) }, 0L, "mapOf(\"a\" to 17, \"b\" to 16, \"c\" to 8).getOrDefault(\"a\", 90)")
        verify("[a]", run { mapOf("a" to 19, "b" to 12, "c" to 5).filter { it.value > 12 }.keys.sorted() }, 0L, "mapOf(\"a\" to 19, \"b\" to 12, \"c\" to 5).filter { it.value > 12 }.keys.sorted()")
        verify("[a, b]", run { mapOf("a" to 16, "b" to 14, "c" to 2, "d" to 5).filter { it.value > 5 }.keys.sorted() }, 0L, "mapOf(\"a\" to 16, \"b\" to 14, \"c\" to 2, \"d\" to 5).filter { it.value > 5 }.keys.sorted()")
        verify("19", run { mapOf("a" to 13, "b" to 15, "c" to 19).getOrDefault("c", 83) }, 0L, "mapOf(\"a\" to 13, \"b\" to 15, \"c\" to 19).getOrDefault(\"c\", 83)")
        verify("[]", run { mapOf("a" to 7, "b" to 11, "c" to 2, "d" to 17).filter { it.value > 17 }.keys.sorted() }, 0L, "mapOf(\"a\" to 7, \"b\" to 11, \"c\" to 2, \"d\" to 17).filter { it.value > 17 }.keys.sorted()")
        verify("68", run { mapOf("x" to 3, "y" to 9, "z" to 5).mapValues { it.value * 4 }.values.sum() }, 0L, "mapOf(\"x\" to 3, \"y\" to 9, \"z\" to 5).mapValues { it.value * 4 }.values.sum()")
        verify("11", run { mapOf("a" to 11, "b" to 11, "c" to 8).getOrDefault("a", 74) }, 0L, "mapOf(\"a\" to 11, \"b\" to 11, \"c\" to 8).getOrDefault(\"a\", 74)")
        verify("[]", run { mapOf("a" to 10, "b" to 14, "c" to 14).filter { it.value > 14 }.keys.sorted() }, 0L, "mapOf(\"a\" to 10, \"b\" to 14, \"c\" to 14).filter { it.value > 14 }.keys.sorted()")
        verify("1", run { mapOf("a" to 2, "b" to 6, "c" to 1).getOrDefault("c", 56) }, 0L, "mapOf(\"a\" to 2, \"b\" to 6, \"c\" to 1).getOrDefault(\"c\", 56)")
        verify("7", run { mapOf("a" to 12, "b" to 7, "c" to 5).getOrDefault("b", 50) }, 0L, "mapOf(\"a\" to 12, \"b\" to 7, \"c\" to 5).getOrDefault(\"b\", 50)")
        verify("92", run { mapOf("x" to 6, "y" to 9, "z" to 8).mapValues { it.value * 4 }.values.sum() }, 0L, "mapOf(\"x\" to 6, \"y\" to 9, \"z\" to 8).mapValues { it.value * 4 }.values.sum()")
        verify("4", run { mapOf("a" to 4, "b" to 1, "c" to 9).getOrDefault("a", 84) }, 0L, "mapOf(\"a\" to 4, \"b\" to 1, \"c\" to 9).getOrDefault(\"a\", 84)")
        verify("[]", run { mapOf("a" to 15, "b" to 12, "c" to 8, "d" to 9).filter { it.value > 15 }.keys.sorted() }, 0L, "mapOf(\"a\" to 15, \"b\" to 12, \"c\" to 8, \"d\" to 9).filter { it.value > 15 }.keys.sorted()")
        verify("36", run { mapOf("x" to 8, "y" to 4, "z" to 6).mapValues { it.value * 2 }.values.sum() }, 0L, "mapOf(\"x\" to 8, \"y\" to 4, \"z\" to 6).mapValues { it.value * 2 }.values.sum()")
    }

    @Test
    fun `verify range_expression batch 0`() {
        verify("9", run { (2..7).filter { it % 3 == 0 }.sum() }, 0L, "(2..7).filter { it % 3 == 0 }.sum()")
        verify("18", run { (3..11).filter { it % 3 == 0 }.sum() }, 0L, "(3..11).filter { it % 3 == 0 }.sum()")
        verify("4", run { (4..19 step 4).count() }, 0L, "(4..19 step 4).count()")
        verify("9", run { (3..8).filter { it % 3 == 0 }.sum() }, 0L, "(3..8).filter { it % 3 == 0 }.sum()")
        verify("12", run { (1..6).filter { it % 2 == 0 }.sum() }, 0L, "(1..6).filter { it % 2 == 0 }.sum()")
        verify("8", run { (8 downTo 3).take(1).last() }, 0L, "(8 downTo 3).take(1).last()")
        verify("20", run { (1..8).filter { it % 2 == 0 }.sum() }, 0L, "(1..8).filter { it % 2 == 0 }.sum()")
        verify("7", run { (9 downTo 4).take(3).last() }, 0L, "(9 downTo 4).take(3).last()")
        verify("3", run { (1..12 step 4).count() }, 0L, "(1..12 step 4).count()")
        verify("4", run { (3..16 step 4).count() }, 0L, "(3..16 step 4).count()")
        verify("12", run { (2..7).filter { it % 2 == 0 }.sum() }, 0L, "(2..7).filter { it % 2 == 0 }.sum()")
        verify("9", run { (11 downTo 7).take(3).last() }, 0L, "(11 downTo 7).take(3).last()")
        verify("8", run { (1..24 step 3).count() }, 0L, "(1..24 step 3).count()")
        verify("12", run { (3..26 step 2).count() }, 0L, "(3..26 step 2).count()")
        verify("6", run { (4..21 step 3).count() }, 0L, "(4..21 step 3).count()")
        verify("11", run { (0..21 step 2).count() }, 0L, "(0..21 step 2).count()")
        verify("18", run { (2..11).filter { it % 3 == 0 }.sum() }, 0L, "(2..11).filter { it % 3 == 0 }.sum()")
        verify("12", run { (14 downTo 8).take(3).last() }, 0L, "(14 downTo 8).take(3).last()")
        verify("5", run { (0..16 step 4).count() }, 0L, "(0..16 step 4).count()")
        verify("10", run { (0..18 step 2).count() }, 0L, "(0..18 step 2).count()")
        verify("12", run { (1..6).filter { it % 2 == 0 }.sum() }, 0L, "(1..6).filter { it % 2 == 0 }.sum()")
        verify("28", run { (4..10).filter { it % 2 == 0 }.sum() }, 0L, "(4..10).filter { it % 2 == 0 }.sum()")
        verify("7", run { (9 downTo 4).take(3).last() }, 0L, "(9 downTo 4).take(3).last()")
        verify("28", run { (4..11).filter { it % 2 == 0 }.sum() }, 0L, "(4..11).filter { it % 2 == 0 }.sum()")
        verify("12", run { (2..7).filter { it % 2 == 0 }.sum() }, 0L, "(2..7).filter { it % 2 == 0 }.sum()")
        verify("40", run { (3..13).filter { it % 2 == 0 }.sum() }, 0L, "(3..13).filter { it % 2 == 0 }.sum()")
        verify("10", run { (11 downTo 1).take(2).last() }, 0L, "(11 downTo 1).take(2).last()")
        verify("18", run { (3..9).filter { it % 2 == 0 }.sum() }, 0L, "(3..9).filter { it % 2 == 0 }.sum()")
        verify("30", run { (1..12).filter { it % 3 == 0 }.sum() }, 0L, "(1..12).filter { it % 3 == 0 }.sum()")
        verify("13", run { (14 downTo 4).take(2).last() }, 0L, "(14 downTo 4).take(2).last()")
        verify("7", run { (2..22 step 3).count() }, 0L, "(2..22 step 3).count()")
        verify("4", run { (3..14 step 3).count() }, 0L, "(3..14 step 3).count()")
        verify("9", run { (9 downTo 5).take(1).last() }, 0L, "(9 downTo 5).take(1).last()")
        verify("54", run { (3..14).filter { it % 2 == 0 }.sum() }, 0L, "(3..14).filter { it % 2 == 0 }.sum()")
        verify("6", run { (8 downTo 1).take(3).last() }, 0L, "(8 downTo 1).take(3).last()")
        verify("18", run { (3..8).filter { it % 2 == 0 }.sum() }, 0L, "(3..8).filter { it % 2 == 0 }.sum()")
        verify("8", run { (8 downTo 2).take(1).last() }, 0L, "(8 downTo 2).take(1).last()")
        verify("27", run { (4..12).filter { it % 3 == 0 }.sum() }, 0L, "(4..12).filter { it % 3 == 0 }.sum()")
        verify("7", run { (0..24 step 4).count() }, 0L, "(0..24 step 4).count()")
        verify("7", run { (1..13 step 2).count() }, 0L, "(1..13 step 2).count()")
        verify("30", run { (2..13).filter { it % 3 == 0 }.sum() }, 0L, "(2..13).filter { it % 3 == 0 }.sum()")
        verify("5", run { (4..22 step 4).count() }, 0L, "(4..22 step 4).count()")
        verify("12", run { (14 downTo 2).take(3).last() }, 0L, "(14 downTo 2).take(3).last()")
        verify("18", run { (3..9).filter { it % 3 == 0 }.sum() }, 0L, "(3..9).filter { it % 3 == 0 }.sum()")
        verify("14", run { (14 downTo 1).take(1).last() }, 0L, "(14 downTo 1).take(1).last()")
        verify("6", run { (2..19 step 3).count() }, 0L, "(2..19 step 3).count()")
        verify("14", run { (14 downTo 4).take(1).last() }, 0L, "(14 downTo 4).take(1).last()")
        verify("40", run { (4..12).filter { it % 2 == 0 }.sum() }, 0L, "(4..12).filter { it % 2 == 0 }.sum()")
        verify("30", run { (2..11).filter { it % 2 == 0 }.sum() }, 0L, "(2..11).filter { it % 2 == 0 }.sum()")
        verify("4", run { (0..14 step 4).count() }, 0L, "(0..14 step 4).count()")
    }

    @Test
    fun `verify range_expression batch 1`() {
        verify("7", run { (1..20 step 3).count() }, 0L, "(1..20 step 3).count()")
        verify("28", run { (3..10).filter { it % 2 == 0 }.sum() }, 0L, "(3..10).filter { it % 2 == 0 }.sum()")
        verify("5", run { (4..21 step 4).count() }, 0L, "(4..21 step 4).count()")
        verify("15", run { (4..11).filter { it % 3 == 0 }.sum() }, 0L, "(4..11).filter { it % 3 == 0 }.sum()")
        verify("18", run { (1..11).filter { it % 3 == 0 }.sum() }, 0L, "(1..11).filter { it % 3 == 0 }.sum()")
        verify("27", run { (4..12).filter { it % 3 == 0 }.sum() }, 0L, "(4..12).filter { it % 3 == 0 }.sum()")
        verify("12", run { (2..24 step 2).count() }, 0L, "(2..24 step 2).count()")
        verify("18", run { (3..9).filter { it % 3 == 0 }.sum() }, 0L, "(3..9).filter { it % 3 == 0 }.sum()")
        verify("5", run { (0..16 step 4).count() }, 0L, "(0..16 step 4).count()")
        verify("7", run { (0..20 step 3).count() }, 0L, "(0..20 step 3).count()")
        verify("8", run { (10 downTo 6).take(3).last() }, 0L, "(10 downTo 6).take(3).last()")
        verify("54", run { (4..15).filter { it % 2 == 0 }.sum() }, 0L, "(4..15).filter { it % 2 == 0 }.sum()")
        verify("6", run { (1..21 step 4).count() }, 0L, "(1..21 step 4).count()")
        verify("54", run { (4..14).filter { it % 2 == 0 }.sum() }, 0L, "(4..14).filter { it % 2 == 0 }.sum()")
        verify("8", run { (1..15 step 2).count() }, 0L, "(1..15 step 2).count()")
        verify("5", run { (0..13 step 3).count() }, 0L, "(0..13 step 3).count()")
        verify("11", run { (13 downTo 5).take(3).last() }, 0L, "(13 downTo 5).take(3).last()")
        verify("20", run { (2..8).filter { it % 2 == 0 }.sum() }, 0L, "(2..8).filter { it % 2 == 0 }.sum()")
        verify("12", run { (13 downTo 2).take(2).last() }, 0L, "(13 downTo 2).take(2).last()")
        verify("7", run { (8 downTo 3).take(2).last() }, 0L, "(8 downTo 3).take(2).last()")
        verify("42", run { (4..15).filter { it % 3 == 0 }.sum() }, 0L, "(4..15).filter { it % 3 == 0 }.sum()")
        verify("30", run { (1..12).filter { it % 3 == 0 }.sum() }, 0L, "(1..12).filter { it % 3 == 0 }.sum()")
        verify("6", run { (8 downTo 1).take(3).last() }, 0L, "(8 downTo 1).take(3).last()")
        verify("11", run { (13 downTo 6).take(3).last() }, 0L, "(13 downTo 6).take(3).last()")
        verify("8", run { (9 downTo 2).take(2).last() }, 0L, "(9 downTo 2).take(2).last()")
        verify("30", run { (3..14).filter { it % 3 == 0 }.sum() }, 0L, "(3..14).filter { it % 3 == 0 }.sum()")
        verify("28", run { (4..11).filter { it % 2 == 0 }.sum() }, 0L, "(4..11).filter { it % 2 == 0 }.sum()")
        verify("12", run { (1..6).filter { it % 2 == 0 }.sum() }, 0L, "(1..6).filter { it % 2 == 0 }.sum()")
        verify("10", run { (3..21 step 2).count() }, 0L, "(3..21 step 2).count()")
        verify("12", run { (0..22 step 2).count() }, 0L, "(0..22 step 2).count()")
        verify("5", run { (2..15 step 3).count() }, 0L, "(2..15 step 3).count()")
        verify("6", run { (3..19 step 3).count() }, 0L, "(3..19 step 3).count()")
        verify("7", run { (8 downTo 4).take(2).last() }, 0L, "(8 downTo 4).take(2).last()")
        verify("6", run { (4..24 step 4).count() }, 0L, "(4..24 step 4).count()")
        verify("27", run { (4..12).filter { it % 3 == 0 }.sum() }, 0L, "(4..12).filter { it % 3 == 0 }.sum()")
        verify("9", run { (9 downTo 2).take(1).last() }, 0L, "(9 downTo 2).take(1).last()")
        verify("10", run { (12 downTo 5).take(3).last() }, 0L, "(12 downTo 5).take(3).last()")
        verify("30", run { (1..12).filter { it % 3 == 0 }.sum() }, 0L, "(1..12).filter { it % 3 == 0 }.sum()")
        verify("9", run { (3..8).filter { it % 3 == 0 }.sum() }, 0L, "(3..8).filter { it % 3 == 0 }.sum()")
        verify("12", run { (14 downTo 9).take(3).last() }, 0L, "(14 downTo 9).take(3).last()")
        verify("18", run { (3..11).filter { it % 3 == 0 }.sum() }, 0L, "(3..11).filter { it % 3 == 0 }.sum()")
        verify("7", run { (2..20 step 3).count() }, 0L, "(2..20 step 3).count()")
        verify("10", run { (10 downTo 2).take(1).last() }, 0L, "(10 downTo 2).take(1).last()")
        verify("30", run { (1..11).filter { it % 2 == 0 }.sum() }, 0L, "(1..11).filter { it % 2 == 0 }.sum()")
        verify("12", run { (13 downTo 4).take(2).last() }, 0L, "(13 downTo 4).take(2).last()")
        verify("12", run { (13 downTo 2).take(2).last() }, 0L, "(13 downTo 2).take(2).last()")
        verify("7", run { (1..19 step 3).count() }, 0L, "(1..19 step 3).count()")
        verify("11", run { (12 downTo 1).take(2).last() }, 0L, "(12 downTo 1).take(2).last()")
        verify("18", run { (3..9).filter { it % 3 == 0 }.sum() }, 0L, "(3..9).filter { it % 3 == 0 }.sum()")
        verify("6", run { (1..23 step 4).count() }, 0L, "(1..23 step 4).count()")
    }

    @Test
    fun `verify pair_destructuring batch 0`() {
        verify("14 11", run {
            val (a, b) = 11 to 14
            "$b $a"
        }, 0L, "val (a, b) = 11 to 14\n\"\$b \$a\"")
        verify("14 7", run {
            val (a, b) = 7 to 14
            "$b $a"
        }, 0L, "val (a, b) = 7 to 14\n\"\$b \$a\"")
        verify("34", run {
            val (x, _, z) = Triple(7, 10, 27)
            x + z
        }, 0L, "val (x, _, z) = Triple(7, 10, 27)\nx + z")
        verify("11 14", run {
            val (a, b) = 14 to 11
            "$b $a"
        }, 0L, "val (a, b) = 14 to 11\n\"\$b \$a\"")
        verify("2 12", run {
            val (a, b) = 12 to 2
            "$b $a"
        }, 0L, "val (a, b) = 12 to 2\n\"\$b \$a\"")
        verify("7", run {
            val pair = 1 to 3
            val (first, second) = pair
            first * second + first + second
        }, 0L, "val pair = 1 to 3\nval (first, second) = pair\nfirst * second + first + second")
        verify("13 8", run {
            val (a, b) = 8 to 13
            "$b $a"
        }, 0L, "val (a, b) = 8 to 13\n\"\$b \$a\"")
        verify("26", run {
            val pair = 2 to 8
            val (first, second) = pair
            first * second + first + second
        }, 0L, "val pair = 2 to 8\nval (first, second) = pair\nfirst * second + first + second")
        verify("29", run {
            val (x, _, z) = Triple(4, 11, 25)
            x + z
        }, 0L, "val (x, _, z) = Triple(4, 11, 25)\nx + z")
        verify("28", run {
            val (x, _, z) = Triple(6, 18, 22)
            x + z
        }, 0L, "val (x, _, z) = Triple(6, 18, 22)\nx + z")
        verify("13 5", run {
            val (a, b) = 5 to 13
            "$b $a"
        }, 0L, "val (a, b) = 5 to 13\n\"\$b \$a\"")
        verify("179", run {
            val pair = 11 to 14
            val (first, second) = pair
            first * second + first + second
        }, 0L, "val pair = 11 to 14\nval (first, second) = pair\nfirst * second + first + second")
        verify("33", run {
            val (x, _, z) = Triple(4, 13, 29)
            x + z
        }, 0L, "val (x, _, z) = Triple(4, 13, 29)\nx + z")
        verify("25", run {
            val (x, _, z) = Triple(1, 18, 24)
            x + z
        }, 0L, "val (x, _, z) = Triple(1, 18, 24)\nx + z")
        verify("26", run {
            val (x, _, z) = Triple(1, 12, 25)
            x + z
        }, 0L, "val (x, _, z) = Triple(1, 12, 25)\nx + z")
        verify("29", run {
            val (x, _, z) = Triple(3, 11, 26)
            x + z
        }, 0L, "val (x, _, z) = Triple(3, 11, 26)\nx + z")
        verify("10 19", run {
            val (a, b) = 19 to 10
            "$b $a"
        }, 0L, "val (a, b) = 19 to 10\n\"\$b \$a\"")
        verify("224", run {
            val pair = 14 to 14
            val (first, second) = pair
            first * second + first + second
        }, 0L, "val pair = 14 to 14\nval (first, second) = pair\nfirst * second + first + second")
        verify("31", run {
            val (x, _, z) = Triple(8, 11, 23)
            x + z
        }, 0L, "val (x, _, z) = Triple(8, 11, 23)\nx + z")
        verify("33", run {
            val (x, _, z) = Triple(5, 18, 28)
            x + z
        }, 0L, "val (x, _, z) = Triple(5, 18, 28)\nx + z")
        verify("4 3", run {
            val (a, b) = 3 to 4
            "$b $a"
        }, 0L, "val (a, b) = 3 to 4\n\"\$b \$a\"")
        verify("19 6", run {
            val (a, b) = 6 to 19
            "$b $a"
        }, 0L, "val (a, b) = 6 to 19\n\"\$b \$a\"")
        verify("89", run {
            val pair = 9 to 8
            val (first, second) = pair
            first * second + first + second
        }, 0L, "val pair = 9 to 8\nval (first, second) = pair\nfirst * second + first + second")
        verify("6 1", run {
            val (a, b) = 1 to 6
            "$b $a"
        }, 0L, "val (a, b) = 1 to 6\n\"\$b \$a\"")
        verify("10 2", run {
            val (a, b) = 2 to 10
            "$b $a"
        }, 0L, "val (a, b) = 2 to 10\n\"\$b \$a\"")
        verify("5 11", run {
            val (a, b) = 11 to 5
            "$b $a"
        }, 0L, "val (a, b) = 11 to 5\n\"\$b \$a\"")
        verify("44", run {
            val pair = 4 to 8
            val (first, second) = pair
            first * second + first + second
        }, 0L, "val pair = 4 to 8\nval (first, second) = pair\nfirst * second + first + second")
        verify("11 7", run {
            val (a, b) = 7 to 11
            "$b $a"
        }, 0L, "val (a, b) = 7 to 11\n\"\$b \$a\"")
        verify("7 8", run {
            val (a, b) = 8 to 7
            "$b $a"
        }, 0L, "val (a, b) = 8 to 7\n\"\$b \$a\"")
        verify("71", run {
            val pair = 7 to 8
            val (first, second) = pair
            first * second + first + second
        }, 0L, "val pair = 7 to 8\nval (first, second) = pair\nfirst * second + first + second")
        verify("33", run {
            val (x, _, z) = Triple(4, 15, 29)
            x + z
        }, 0L, "val (x, _, z) = Triple(4, 15, 29)\nx + z")
        verify("37", run {
            val (x, _, z) = Triple(9, 16, 28)
            x + z
        }, 0L, "val (x, _, z) = Triple(9, 16, 28)\nx + z")
        verify("139", run {
            val pair = 9 to 13
            val (first, second) = pair
            first * second + first + second
        }, 0L, "val pair = 9 to 13\nval (first, second) = pair\nfirst * second + first + second")
        verify("4 1", run {
            val (a, b) = 1 to 4
            "$b $a"
        }, 0L, "val (a, b) = 1 to 4\n\"\$b \$a\"")
        verify("44", run {
            val pair = 8 to 4
            val (first, second) = pair
            first * second + first + second
        }, 0L, "val pair = 8 to 4\nval (first, second) = pair\nfirst * second + first + second")
        verify("12 14", run {
            val (a, b) = 14 to 12
            "$b $a"
        }, 0L, "val (a, b) = 14 to 12\n\"\$b \$a\"")
        verify("15", run {
            val pair = 1 to 7
            val (first, second) = pair
            first * second + first + second
        }, 0L, "val pair = 1 to 7\nval (first, second) = pair\nfirst * second + first + second")
        verify("6 10", run {
            val (a, b) = 10 to 6
            "$b $a"
        }, 0L, "val (a, b) = 10 to 6\n\"\$b \$a\"")
        verify("30", run {
            val (x, _, z) = Triple(6, 19, 24)
            x + z
        }, 0L, "val (x, _, z) = Triple(6, 19, 24)\nx + z")
        verify("26", run {
            val (x, _, z) = Triple(3, 12, 23)
            x + z
        }, 0L, "val (x, _, z) = Triple(3, 12, 23)\nx + z")
        verify("6 5", run {
            val (a, b) = 5 to 6
            "$b $a"
        }, 0L, "val (a, b) = 5 to 6\n\"\$b \$a\"")
        verify("24", run {
            val (x, _, z) = Triple(3, 13, 21)
            x + z
        }, 0L, "val (x, _, z) = Triple(3, 13, 21)\nx + z")
        verify("119", run {
            val pair = 7 to 14
            val (first, second) = pair
            first * second + first + second
        }, 0L, "val pair = 7 to 14\nval (first, second) = pair\nfirst * second + first + second")
        verify("5 10", run {
            val (a, b) = 10 to 5
            "$b $a"
        }, 0L, "val (a, b) = 10 to 5\n\"\$b \$a\"")
        verify("119", run {
            val pair = 14 to 7
            val (first, second) = pair
            first * second + first + second
        }, 0L, "val pair = 14 to 7\nval (first, second) = pair\nfirst * second + first + second")
        verify("36", run {
            val (x, _, z) = Triple(9, 17, 27)
            x + z
        }, 0L, "val (x, _, z) = Triple(9, 17, 27)\nx + z")
        verify("55", run {
            val pair = 7 to 6
            val (first, second) = pair
            first * second + first + second
        }, 0L, "val pair = 7 to 6\nval (first, second) = pair\nfirst * second + first + second")
        verify("8 6", run {
            val (a, b) = 6 to 8
            "$b $a"
        }, 0L, "val (a, b) = 6 to 8\n\"\$b \$a\"")
        verify("17 5", run {
            val (a, b) = 5 to 17
            "$b $a"
        }, 0L, "val (a, b) = 5 to 17\n\"\$b \$a\"")
        verify("22", run {
            val (x, _, z) = Triple(1, 14, 21)
            x + z
        }, 0L, "val (x, _, z) = Triple(1, 14, 21)\nx + z")
    }

    @Test
    fun `verify pair_destructuring batch 1`() {
        verify("31", run {
            val (x, _, z) = Triple(5, 14, 26)
            x + z
        }, 0L, "val (x, _, z) = Triple(5, 14, 26)\nx + z")
        verify("13 4", run {
            val (a, b) = 4 to 13
            "$b $a"
        }, 0L, "val (a, b) = 4 to 13\n\"\$b \$a\"")
        verify("30", run {
            val (x, _, z) = Triple(2, 17, 28)
            x + z
        }, 0L, "val (x, _, z) = Triple(2, 17, 28)\nx + z")
        verify("16 19", run {
            val (a, b) = 19 to 16
            "$b $a"
        }, 0L, "val (a, b) = 19 to 16\n\"\$b \$a\"")
        verify("17 10", run {
            val (a, b) = 10 to 17
            "$b $a"
        }, 0L, "val (a, b) = 10 to 17\n\"\$b \$a\"")
        verify("6 6", run {
            val (a, b) = 6 to 6
            "$b $a"
        }, 0L, "val (a, b) = 6 to 6\n\"\$b \$a\"")
        verify("33", run {
            val (x, _, z) = Triple(7, 17, 26)
            x + z
        }, 0L, "val (x, _, z) = Triple(7, 17, 26)\nx + z")
        verify("13 15", run {
            val (a, b) = 15 to 13
            "$b $a"
        }, 0L, "val (a, b) = 15 to 13\n\"\$b \$a\"")
        verify("35", run {
            val (x, _, z) = Triple(9, 16, 26)
            x + z
        }, 0L, "val (x, _, z) = Triple(9, 16, 26)\nx + z")
        verify("25", run {
            val (x, _, z) = Triple(2, 15, 23)
            x + z
        }, 0L, "val (x, _, z) = Triple(2, 15, 23)\nx + z")
        verify("43", run {
            val pair = 3 to 10
            val (first, second) = pair
            first * second + first + second
        }, 0L, "val pair = 3 to 10\nval (first, second) = pair\nfirst * second + first + second")
        verify("6 14", run {
            val (a, b) = 14 to 6
            "$b $a"
        }, 0L, "val (a, b) = 14 to 6\n\"\$b \$a\"")
        verify("30", run {
            val (x, _, z) = Triple(4, 10, 26)
            x + z
        }, 0L, "val (x, _, z) = Triple(4, 10, 26)\nx + z")
        verify("3 19", run {
            val (a, b) = 19 to 3
            "$b $a"
        }, 0L, "val (a, b) = 19 to 3\n\"\$b \$a\"")
        verify("27", run {
            val (x, _, z) = Triple(2, 14, 25)
            x + z
        }, 0L, "val (x, _, z) = Triple(2, 14, 25)\nx + z")
        verify("31", run {
            val (x, _, z) = Triple(5, 13, 26)
            x + z
        }, 0L, "val (x, _, z) = Triple(5, 13, 26)\nx + z")
        verify("90", run {
            val pair = 6 to 12
            val (first, second) = pair
            first * second + first + second
        }, 0L, "val pair = 6 to 12\nval (first, second) = pair\nfirst * second + first + second")
        verify("13 6", run {
            val (a, b) = 6 to 13
            "$b $a"
        }, 0L, "val (a, b) = 6 to 13\n\"\$b \$a\"")
        verify("55", run {
            val pair = 13 to 3
            val (first, second) = pair
            first * second + first + second
        }, 0L, "val pair = 13 to 3\nval (first, second) = pair\nfirst * second + first + second")
        verify("21", run {
            val pair = 1 to 10
            val (first, second) = pair
            first * second + first + second
        }, 0L, "val pair = 1 to 10\nval (first, second) = pair\nfirst * second + first + second")
        verify("15 3", run {
            val (a, b) = 3 to 15
            "$b $a"
        }, 0L, "val (a, b) = 3 to 15\n\"\$b \$a\"")
        verify("1 8", run {
            val (a, b) = 8 to 1
            "$b $a"
        }, 0L, "val (a, b) = 8 to 1\n\"\$b \$a\"")
        verify("71", run {
            val pair = 8 to 7
            val (first, second) = pair
            first * second + first + second
        }, 0L, "val pair = 8 to 7\nval (first, second) = pair\nfirst * second + first + second")
        verify("34", run {
            val pair = 6 to 4
            val (first, second) = pair
            first * second + first + second
        }, 0L, "val pair = 6 to 4\nval (first, second) = pair\nfirst * second + first + second")
        verify("129", run {
            val pair = 9 to 12
            val (first, second) = pair
            first * second + first + second
        }, 0L, "val pair = 9 to 12\nval (first, second) = pair\nfirst * second + first + second")
        verify("6 18", run {
            val (a, b) = 18 to 6
            "$b $a"
        }, 0L, "val (a, b) = 18 to 6\n\"\$b \$a\"")
        verify("9 8", run {
            val (a, b) = 8 to 9
            "$b $a"
        }, 0L, "val (a, b) = 8 to 9\n\"\$b \$a\"")
        verify("12 8", run {
            val (a, b) = 8 to 12
            "$b $a"
        }, 0L, "val (a, b) = 8 to 12\n\"\$b \$a\"")
        verify("25", run {
            val (x, _, z) = Triple(4, 13, 21)
            x + z
        }, 0L, "val (x, _, z) = Triple(4, 13, 21)\nx + z")
        verify("24", run {
            val (x, _, z) = Triple(2, 12, 22)
            x + z
        }, 0L, "val (x, _, z) = Triple(2, 12, 22)\nx + z")
        verify("32", run {
            val (x, _, z) = Triple(9, 13, 23)
            x + z
        }, 0L, "val (x, _, z) = Triple(9, 13, 23)\nx + z")
        verify("35", run {
            val (x, _, z) = Triple(7, 16, 28)
            x + z
        }, 0L, "val (x, _, z) = Triple(7, 16, 28)\nx + z")
        verify("15", run {
            val pair = 1 to 7
            val (first, second) = pair
            first * second + first + second
        }, 0L, "val pair = 1 to 7\nval (first, second) = pair\nfirst * second + first + second")
        verify("29", run {
            val (x, _, z) = Triple(1, 15, 28)
            x + z
        }, 0L, "val (x, _, z) = Triple(1, 15, 28)\nx + z")
        verify("4 2", run {
            val (a, b) = 2 to 4
            "$b $a"
        }, 0L, "val (a, b) = 2 to 4\n\"\$b \$a\"")
        verify("23", run {
            val pair = 2 to 7
            val (first, second) = pair
            first * second + first + second
        }, 0L, "val pair = 2 to 7\nval (first, second) = pair\nfirst * second + first + second")
        verify("83", run {
            val pair = 5 to 13
            val (first, second) = pair
            first * second + first + second
        }, 0L, "val pair = 5 to 13\nval (first, second) = pair\nfirst * second + first + second")
        verify("19 2", run {
            val (a, b) = 2 to 19
            "$b $a"
        }, 0L, "val (a, b) = 2 to 19\n\"\$b \$a\"")
        verify("16 5", run {
            val (a, b) = 5 to 16
            "$b $a"
        }, 0L, "val (a, b) = 5 to 16\n\"\$b \$a\"")
        verify("31", run {
            val pair = 7 to 3
            val (first, second) = pair
            first * second + first + second
        }, 0L, "val pair = 7 to 3\nval (first, second) = pair\nfirst * second + first + second")
        verify("7 15", run {
            val (a, b) = 15 to 7
            "$b $a"
        }, 0L, "val (a, b) = 15 to 7\n\"\$b \$a\"")
        verify("23", run {
            val (x, _, z) = Triple(3, 13, 20)
            x + z
        }, 0L, "val (x, _, z) = Triple(3, 13, 20)\nx + z")
        verify("19", run {
            val pair = 3 to 4
            val (first, second) = pair
            first * second + first + second
        }, 0L, "val pair = 3 to 4\nval (first, second) = pair\nfirst * second + first + second")
        verify("10 18", run {
            val (a, b) = 18 to 10
            "$b $a"
        }, 0L, "val (a, b) = 18 to 10\n\"\$b \$a\"")
        verify("55", run {
            val pair = 6 to 7
            val (first, second) = pair
            first * second + first + second
        }, 0L, "val pair = 6 to 7\nval (first, second) = pair\nfirst * second + first + second")
        verify("20", run {
            val pair = 6 to 2
            val (first, second) = pair
            first * second + first + second
        }, 0L, "val pair = 6 to 2\nval (first, second) = pair\nfirst * second + first + second")
        verify("30", run {
            val (x, _, z) = Triple(6, 13, 24)
            x + z
        }, 0L, "val (x, _, z) = Triple(6, 13, 24)\nx + z")
        verify("71", run {
            val pair = 5 to 11
            val (first, second) = pair
            first * second + first + second
        }, 0L, "val pair = 5 to 11\nval (first, second) = pair\nfirst * second + first + second")
        verify("15 1", run {
            val (a, b) = 1 to 15
            "$b $a"
        }, 0L, "val (a, b) = 1 to 15\n\"\$b \$a\"")
        verify("37", run {
            val (x, _, z) = Triple(8, 17, 29)
            x + z
        }, 0L, "val (x, _, z) = Triple(8, 17, 29)\nx + z")
    }

    @Test
    fun `verify null_safety batch 0`() {
        verify("5", run {
            val s: String? = "world"
            s?.length ?: -3
        }, 0L, "val s: String? = \"world\"\ns?.length ?: -3")
        verify("-3", run {
            val s: String? = null
            s?.length ?: -3
        }, 0L, "val s: String? = null\ns?.length ?: -3")
        verify("24", run {
            val x: Int? = 12
            x?.let { it * 2 } ?: 40
        }, 0L, "val x: Int? = 12\nx?.let { it * 2 } ?: 40")
        verify("-2", run {
            val s: String? = null
            s?.length ?: -2
        }, 0L, "val s: String? = null\ns?.length ?: -2")
        verify("6", run {
            val s: String? = "kotlin"
            s?.length ?: -1
        }, 0L, "val s: String? = \"kotlin\"\ns?.length ?: -1")
        verify("null", run {
            val list: List<Int>? = null
            list?.firstOrNull()?.plus(1)
        }, 0L, "val list: List<Int>? = null\nlist?.firstOrNull()?.plus(1)")
        verify("6", run {
            val s: String? = "kotlin"
            s?.length ?: -3
        }, 0L, "val s: String? = \"kotlin\"\ns?.length ?: -3")
        verify("13", run {
            val list: List<Int>? = listOf(12, 3)
            list?.firstOrNull()?.plus(1)
        }, 0L, "val list: List<Int>? = listOf(12, 3)\nlist?.firstOrNull()?.plus(1)")
        verify("36", run {
            val x: Int? = 18
            x?.let { it * 2 } ?: 21
        }, 0L, "val x: Int? = 18\nx?.let { it * 2 } ?: 21")
        verify("48", run {
            val x: Int? = null
            x?.let { it * 2 } ?: 48
        }, 0L, "val x: Int? = null\nx?.let { it * 2 } ?: 48")
        verify("4", run {
            val s: String? = "code"
            s?.length ?: -5
        }, 0L, "val s: String? = \"code\"\ns?.length ?: -5")
        verify("null", run {
            val list: List<Int>? = null
            list?.firstOrNull()?.plus(1)
        }, 0L, "val list: List<Int>? = null\nlist?.firstOrNull()?.plus(1)")
        verify("8", run {
            val x: Int? = 4
            x?.let { it * 2 } ?: 13
        }, 0L, "val x: Int? = 4\nx?.let { it * 2 } ?: 13")
        verify("18", run {
            val x: Int? = null
            x?.let { it * 2 } ?: 18
        }, 0L, "val x: Int? = null\nx?.let { it * 2 } ?: 18")
        verify("22", run {
            val x: Int? = 11
            x?.let { it * 2 } ?: 32
        }, 0L, "val x: Int? = 11\nx?.let { it * 2 } ?: 32")
        verify("31", run {
            val x: Int? = null
            x?.let { it * 2 } ?: 31
        }, 0L, "val x: Int? = null\nx?.let { it * 2 } ?: 31")
        verify("5", run {
            val s: String? = "world"
            s?.length ?: -5
        }, 0L, "val s: String? = \"world\"\ns?.length ?: -5")
        verify("null", run {
            val list: List<Int>? = null
            list?.firstOrNull()?.plus(1)
        }, 0L, "val list: List<Int>? = null\nlist?.firstOrNull()?.plus(1)")
        verify("4", run {
            val x: Int? = 2
            x?.let { it * 2 } ?: 41
        }, 0L, "val x: Int? = 2\nx?.let { it * 2 } ?: 41")
        verify("28", run {
            val x: Int? = null
            x?.let { it * 2 } ?: 28
        }, 0L, "val x: Int? = null\nx?.let { it * 2 } ?: 28")
        verify("5", run {
            val s: String? = "hello"
            s?.length ?: -2
        }, 0L, "val s: String? = \"hello\"\ns?.length ?: -2")
        verify("-2", run {
            val s: String? = null
            s?.length ?: -2
        }, 0L, "val s: String? = null\ns?.length ?: -2")
        verify("12", run {
            val list: List<Int>? = listOf(11, 13, 8, 3)
            list?.firstOrNull()?.plus(1)
        }, 0L, "val list: List<Int>? = listOf(11, 13, 8, 3)\nlist?.firstOrNull()?.plus(1)")
        verify("-2", run {
            val s: String? = null
            s?.length ?: -2
        }, 0L, "val s: String? = null\ns?.length ?: -2")
        verify("4", run {
            val s: String? = "test"
            s?.length ?: -3
        }, 0L, "val s: String? = \"test\"\ns?.length ?: -3")
        verify("-2", run {
            val s: String? = null
            s?.length ?: -2
        }, 0L, "val s: String? = null\ns?.length ?: -2")
        verify("4", run {
            val list: List<Int>? = listOf(3, 6, 9, 14)
            list?.firstOrNull()?.plus(1)
        }, 0L, "val list: List<Int>? = listOf(3, 6, 9, 14)\nlist?.firstOrNull()?.plus(1)")
        verify("-3", run {
            val s: String? = null
            s?.length ?: -3
        }, 0L, "val s: String? = null\ns?.length ?: -3")
        verify("6", run {
            val s: String? = "kotlin"
            s?.length ?: -1
        }, 0L, "val s: String? = \"kotlin\"\ns?.length ?: -1")
        verify("null", run {
            val list: List<Int>? = null
            list?.firstOrNull()?.plus(1)
        }, 0L, "val list: List<Int>? = null\nlist?.firstOrNull()?.plus(1)")
        verify("22", run {
            val x: Int? = 11
            x?.let { it * 2 } ?: 35
        }, 0L, "val x: Int? = 11\nx?.let { it * 2 } ?: 35")
        verify("36", run {
            val x: Int? = null
            x?.let { it * 2 } ?: 36
        }, 0L, "val x: Int? = null\nx?.let { it * 2 } ?: 36")
        verify("20", run {
            val list: List<Int>? = listOf(19, 9)
            list?.firstOrNull()?.plus(1)
        }, 0L, "val list: List<Int>? = listOf(19, 9)\nlist?.firstOrNull()?.plus(1)")
        verify("-2", run {
            val s: String? = null
            s?.length ?: -2
        }, 0L, "val s: String? = null\ns?.length ?: -2")
        verify("14", run {
            val list: List<Int>? = listOf(13, 19, 2, 12)
            list?.firstOrNull()?.plus(1)
        }, 0L, "val list: List<Int>? = listOf(13, 19, 2, 12)\nlist?.firstOrNull()?.plus(1)")
        verify("-2", run {
            val s: String? = null
            s?.length ?: -2
        }, 0L, "val s: String? = null\ns?.length ?: -2")
        verify("15", run {
            val list: List<Int>? = listOf(14, 4, 4, 10)
            list?.firstOrNull()?.plus(1)
        }, 0L, "val list: List<Int>? = listOf(14, 4, 4, 10)\nlist?.firstOrNull()?.plus(1)")
        verify("-1", run {
            val s: String? = null
            s?.length ?: -1
        }, 0L, "val s: String? = null\ns?.length ?: -1")
        verify("32", run {
            val x: Int? = 16
            x?.let { it * 2 } ?: 29
        }, 0L, "val x: Int? = 16\nx?.let { it * 2 } ?: 29")
        verify("32", run {
            val x: Int? = null
            x?.let { it * 2 } ?: 32
        }, 0L, "val x: Int? = null\nx?.let { it * 2 } ?: 32")
        verify("5", run {
            val s: String? = "world"
            s?.length ?: -1
        }, 0L, "val s: String? = \"world\"\ns?.length ?: -1")
        verify("13", run {
            val x: Int? = null
            x?.let { it * 2 } ?: 13
        }, 0L, "val x: Int? = null\nx?.let { it * 2 } ?: 13")
        verify("4", run {
            val list: List<Int>? = listOf(3, 17, 10, 2)
            list?.firstOrNull()?.plus(1)
        }, 0L, "val list: List<Int>? = listOf(3, 17, 10, 2)\nlist?.firstOrNull()?.plus(1)")
        verify("-4", run {
            val s: String? = null
            s?.length ?: -4
        }, 0L, "val s: String? = null\ns?.length ?: -4")
        verify("17", run {
            val list: List<Int>? = listOf(16, 18)
            list?.firstOrNull()?.plus(1)
        }, 0L, "val list: List<Int>? = listOf(16, 18)\nlist?.firstOrNull()?.plus(1)")
        verify("37", run {
            val x: Int? = null
            x?.let { it * 2 } ?: 37
        }, 0L, "val x: Int? = null\nx?.let { it * 2 } ?: 37")
        verify("9", run {
            val list: List<Int>? = listOf(8, 9, 17, 1)
            list?.firstOrNull()?.plus(1)
        }, 0L, "val list: List<Int>? = listOf(8, 9, 17, 1)\nlist?.firstOrNull()?.plus(1)")
        verify("-4", run {
            val s: String? = null
            s?.length ?: -4
        }, 0L, "val s: String? = null\ns?.length ?: -4")
        verify("5", run {
            val s: String? = "world"
            s?.length ?: -3
        }, 0L, "val s: String? = \"world\"\ns?.length ?: -3")
        verify("44", run {
            val x: Int? = null
            x?.let { it * 2 } ?: 44
        }, 0L, "val x: Int? = null\nx?.let { it * 2 } ?: 44")
    }

    @Test
    fun `verify null_safety batch 1`() {
        verify("24", run {
            val x: Int? = 12
            x?.let { it * 2 } ?: 14
        }, 0L, "val x: Int? = 12\nx?.let { it * 2 } ?: 14")
        verify("-5", run {
            val s: String? = null
            s?.length ?: -5
        }, 0L, "val s: String? = null\ns?.length ?: -5")
        verify("30", run {
            val x: Int? = 15
            x?.let { it * 2 } ?: 47
        }, 0L, "val x: Int? = 15\nx?.let { it * 2 } ?: 47")
        verify("-3", run {
            val s: String? = null
            s?.length ?: -3
        }, 0L, "val s: String? = null\ns?.length ?: -3")
        verify("5", run {
            val s: String? = "world"
            s?.length ?: -1
        }, 0L, "val s: String? = \"world\"\ns?.length ?: -1")
        verify("-5", run {
            val s: String? = null
            s?.length ?: -5
        }, 0L, "val s: String? = null\ns?.length ?: -5")
        verify("2", run {
            val x: Int? = 1
            x?.let { it * 2 } ?: 17
        }, 0L, "val x: Int? = 1\nx?.let { it * 2 } ?: 17")
        verify("-5", run {
            val s: String? = null
            s?.length ?: -5
        }, 0L, "val s: String? = null\ns?.length ?: -5")
        verify("30", run {
            val x: Int? = 15
            x?.let { it * 2 } ?: 36
        }, 0L, "val x: Int? = 15\nx?.let { it * 2 } ?: 36")
        verify("35", run {
            val x: Int? = null
            x?.let { it * 2 } ?: 35
        }, 0L, "val x: Int? = null\nx?.let { it * 2 } ?: 35")
        verify("6", run {
            val list: List<Int>? = listOf(5, 10, 6, 19)
            list?.firstOrNull()?.plus(1)
        }, 0L, "val list: List<Int>? = listOf(5, 10, 6, 19)\nlist?.firstOrNull()?.plus(1)")
        verify("-4", run {
            val s: String? = null
            s?.length ?: -4
        }, 0L, "val s: String? = null\ns?.length ?: -4")
        verify("18", run {
            val x: Int? = 9
            x?.let { it * 2 } ?: 30
        }, 0L, "val x: Int? = 9\nx?.let { it * 2 } ?: 30")
        verify("-5", run {
            val s: String? = null
            s?.length ?: -5
        }, 0L, "val s: String? = null\ns?.length ?: -5")
        verify("14", run {
            val x: Int? = null
            x?.let { it * 2 } ?: 14
        }, 0L, "val x: Int? = null\nx?.let { it * 2 } ?: 14")
        verify("18", run {
            val x: Int? = 9
            x?.let { it * 2 } ?: 23
        }, 0L, "val x: Int? = 9\nx?.let { it * 2 } ?: 23")
        verify("null", run {
            val list: List<Int>? = null
            list?.firstOrNull()?.plus(1)
        }, 0L, "val list: List<Int>? = null\nlist?.firstOrNull()?.plus(1)")
        verify("5", run {
            val s: String? = "hello"
            s?.length ?: -3
        }, 0L, "val s: String? = \"hello\"\ns?.length ?: -3")
        verify("null", run {
            val list: List<Int>? = null
            list?.firstOrNull()?.plus(1)
        }, 0L, "val list: List<Int>? = null\nlist?.firstOrNull()?.plus(1)")
        verify("6", run {
            val list: List<Int>? = listOf(5, 1, 6, 11)
            list?.firstOrNull()?.plus(1)
        }, 0L, "val list: List<Int>? = listOf(5, 1, 6, 11)\nlist?.firstOrNull()?.plus(1)")
        verify("-3", run {
            val s: String? = null
            s?.length ?: -3
        }, 0L, "val s: String? = null\ns?.length ?: -3")
        verify("5", run {
            val s: String? = "hello"
            s?.length ?: -4
        }, 0L, "val s: String? = \"hello\"\ns?.length ?: -4")
        verify("null", run {
            val list: List<Int>? = null
            list?.firstOrNull()?.plus(1)
        }, 0L, "val list: List<Int>? = null\nlist?.firstOrNull()?.plus(1)")
        verify("2", run {
            val list: List<Int>? = listOf(1, 19, 10, 1)
            list?.firstOrNull()?.plus(1)
        }, 0L, "val list: List<Int>? = listOf(1, 19, 10, 1)\nlist?.firstOrNull()?.plus(1)")
        verify("null", run {
            val list: List<Int>? = null
            list?.firstOrNull()?.plus(1)
        }, 0L, "val list: List<Int>? = null\nlist?.firstOrNull()?.plus(1)")
        verify("-1", run {
            val s: String? = null
            s?.length ?: -1
        }, 0L, "val s: String? = null\ns?.length ?: -1")
        verify("-1", run {
            val s: String? = null
            s?.length ?: -1
        }, 0L, "val s: String? = null\ns?.length ?: -1")
        verify("5", run {
            val s: String? = "hello"
            s?.length ?: -5
        }, 0L, "val s: String? = \"hello\"\ns?.length ?: -5")
        verify("43", run {
            val x: Int? = null
            x?.let { it * 2 } ?: 43
        }, 0L, "val x: Int? = null\nx?.let { it * 2 } ?: 43")
        verify("14", run {
            val x: Int? = 7
            x?.let { it * 2 } ?: 12
        }, 0L, "val x: Int? = 7\nx?.let { it * 2 } ?: 12")
        verify("43", run {
            val x: Int? = null
            x?.let { it * 2 } ?: 43
        }, 0L, "val x: Int? = null\nx?.let { it * 2 } ?: 43")
        verify("30", run {
            val x: Int? = 15
            x?.let { it * 2 } ?: 26
        }, 0L, "val x: Int? = 15\nx?.let { it * 2 } ?: 26")
        verify("null", run {
            val list: List<Int>? = null
            list?.firstOrNull()?.plus(1)
        }, 0L, "val list: List<Int>? = null\nlist?.firstOrNull()?.plus(1)")
        verify("32", run {
            val x: Int? = 16
            x?.let { it * 2 } ?: 45
        }, 0L, "val x: Int? = 16\nx?.let { it * 2 } ?: 45")
        verify("-2", run {
            val s: String? = null
            s?.length ?: -2
        }, 0L, "val s: String? = null\ns?.length ?: -2")
        verify("8", run {
            val list: List<Int>? = listOf(7, 18, 1)
            list?.firstOrNull()?.plus(1)
        }, 0L, "val list: List<Int>? = listOf(7, 18, 1)\nlist?.firstOrNull()?.plus(1)")
        verify("null", run {
            val list: List<Int>? = null
            list?.firstOrNull()?.plus(1)
        }, 0L, "val list: List<Int>? = null\nlist?.firstOrNull()?.plus(1)")
        verify("4", run {
            val s: String? = "code"
            s?.length ?: -4
        }, 0L, "val s: String? = \"code\"\ns?.length ?: -4")
        verify("-5", run {
            val s: String? = null
            s?.length ?: -5
        }, 0L, "val s: String? = null\ns?.length ?: -5")
        verify("20", run {
            val list: List<Int>? = listOf(19, 1, 13, 7)
            list?.firstOrNull()?.plus(1)
        }, 0L, "val list: List<Int>? = listOf(19, 1, 13, 7)\nlist?.firstOrNull()?.plus(1)")
        verify("-5", run {
            val s: String? = null
            s?.length ?: -5
        }, 0L, "val s: String? = null\ns?.length ?: -5")
        verify("34", run {
            val x: Int? = 17
            x?.let { it * 2 } ?: 43
        }, 0L, "val x: Int? = 17\nx?.let { it * 2 } ?: 43")
        verify("null", run {
            val list: List<Int>? = null
            list?.firstOrNull()?.plus(1)
        }, 0L, "val list: List<Int>? = null\nlist?.firstOrNull()?.plus(1)")
        verify("4", run {
            val s: String? = "test"
            s?.length ?: -5
        }, 0L, "val s: String? = \"test\"\ns?.length ?: -5")
        verify("null", run {
            val list: List<Int>? = null
            list?.firstOrNull()?.plus(1)
        }, 0L, "val list: List<Int>? = null\nlist?.firstOrNull()?.plus(1)")
        verify("6", run {
            val list: List<Int>? = listOf(5, 1, 12)
            list?.firstOrNull()?.plus(1)
        }, 0L, "val list: List<Int>? = listOf(5, 1, 12)\nlist?.firstOrNull()?.plus(1)")
        verify("43", run {
            val x: Int? = null
            x?.let { it * 2 } ?: 43
        }, 0L, "val x: Int? = null\nx?.let { it * 2 } ?: 43")
        verify("10", run {
            val list: List<Int>? = listOf(9, 13)
            list?.firstOrNull()?.plus(1)
        }, 0L, "val list: List<Int>? = listOf(9, 13)\nlist?.firstOrNull()?.plus(1)")
        verify("-2", run {
            val s: String? = null
            s?.length ?: -2
        }, 0L, "val s: String? = null\ns?.length ?: -2")
        verify("14", run {
            val x: Int? = 7
            x?.let { it * 2 } ?: 17
        }, 0L, "val x: Int? = 7\nx?.let { it * 2 } ?: 17")
    }
}
